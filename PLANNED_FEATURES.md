# Ycloud 待实施功能清单

> 确认日期：2026-08-19
>
> 状态：方案已确认，尚未实施
> 原则：安全 40%、性能 30%、稳定 30%，面向 1 核 1 GiB VPS。

本文只记录本阶段已经确认的新增功能和明确不开放的设置。实施完成后，应将最终行为同步进 `ARCHITECTURE.md` 和 `README.md`，并删除本文件，避免同时维护“计划”和“现状”两套说明。

## 1. 登录保护设置

在管理员后台的“账户与访问”中加入登录保护设置，并将其与账户密码设置、登录日志分区展示。

### 管理员登录

- 错误次数限制可配置，默认 3 次，允许范围 3～10 次。
- 封禁时间可配置，默认 60 分钟，允许范围 5 分钟～24 小时。
- 达到限制后按真实客户端 IP 封禁。

### 首页登录

- 错误次数限制可配置，默认 5 次，允许范围 3～20 次。
- 封禁时间可配置，默认 60 分钟，允许范围 5 分钟～24 小时。
- 首页与管理员登录的失败计数、封禁状态互相独立。

### 固定安全要求

- 限制状态必须持久化，服务重启后不能自动消失。
- 只在可信代理来源满足既有代理规则时采用代理提供的真实客户端 IP。
- 不记录密码、Cookie、会话令牌、网页访问令牌或 WebDAV 凭据。
- 修改登录限制不能削弱固定的瞬时请求限流和 Argon2 资源保护。
- WebDAV 的失败限制、密码验证队列和并发保护保持程序固定，不放入后台调节。

## 2. 登录安全日志

### 日志分类

后台分成两个页签：

- **正常登录**：记录成功入口、客户端 IP、时间、客户端信息等；每条记录右侧允许管理员手动封禁该 IP。
- **错误登录**：记录失败入口、客户端 IP、时间、失败次数、封禁状态与封禁截止时间；仅对已封禁 IP 提供“解除封禁”。

“解除封禁”只清除该 IP 对应入口的封禁和失败计数，不删除历史日志。

### 查询方式

- 使用服务端游标分页，不一次性把全部日志加载到浏览器。
- 每页默认 20 条，仅提供上一页和下一页，不为显示总页数执行昂贵的全量统计。
- 增加查看时间筛选：今天、最近 3 天、最近 7 天、最近 15 天、最近 30 天、自定义起止时间。
- 默认查看最近 7 天。
- 支持按登录入口、IP 和正常/错误状态筛选。
- 改变筛选条件后从第一页重新查询。

### 保存策略

- 保存天数允许选择 1、3、5、7、15、30 天，默认 7 天。
- 日志最大保存条目允许修改，范围 500～20,000 条，默认 5,000 条。
- 同时满足“保存天数”和“最大条目数”约束；超限时优先删除最旧记录。
- 将最大条目数调低并保存后，清理超出的旧记录。
- 日志不能无限增长，也不能在每次登录时反复重写一个不断扩大的完整 JSON 文件。

### 手动封禁

- 封禁当前管理员正在使用的 IP 前必须二次确认，防止把自己锁在后台之外。
- 页面提示共享出口、NAT 和反向代理场景下封禁 IP 可能影响多名用户。
- 手动封禁后，应撤销该 IP 已存在的管理员会话或首页访问令牌；实现时需要让对应认证状态可以安全关联来源 IP。

## 3. 上传与下载速率限制

在后台增加独立的“传输设置”，不要放进“账户与访问”。

- 全局上传速率限制，单位 MiB/s，`0` 表示不限速。
- 全局下载速率限制，单位 MiB/s，`0` 表示不限速。
- 默认值均为 `0`，保持当前版本行为。
- 网页上传与 WebDAV `PUT` 共用全局上传限速。
- 普通下载、打包下载和 WebDAV `GET` 共用全局下载限速。
- 限速采用流式令牌桶或等价方案，不能把完整文件读入内存。
- 客户端断开后必须及时取消等待和释放资源。
- 内部移动、复制、重命名、删除和事务恢复不计入网络速率限制。
- 单文件上传大小、打包源文件总大小和打包文件数量继续作为独立设置，不与速率限制合并。

## 4. 桌面端拖拽框选

- 仅桌面端启用鼠标左键拖拽框选。
- 从文件浏览区域开始拖动时显示选择矩形，与矩形相交的文件行进入选中状态。
- 拖拽只能选择文件，不能触发文件名、修改时间、大小等文本选择。
- 设置合理的移动阈值，普通单击不能被误判为拖拽。
- 松开鼠标后只保留选中状态，不自动弹出任何操作框。
- 保留复选框选择和现有右键操作。
- 不影响单击、双击打开和右键菜单；不实现外部文件拖入浏览器上传。
- 上传入口只保留右上角上传图标和文件浏览框空白区域的右键菜单。
- 手机端不启用拖拽框选，继续使用复选框，避免滚动时误选。

## 5. 右键菜单范围

- 在文件或已选文件上右键：显示与当前选择相适用的下载、打包下载、移动、复制、删除等操作。
- 在文件浏览框内但没有命中文件行的空白区域右键：只显示“新建文件夹”和“上传文件”。
- 在整个文件浏览框之外右键：不拦截，保留浏览器原生右键菜单。
- 未登录管理员时，不显示新建、上传、移动、复制、删除等写入操作。
- 自定义菜单必须按鼠标位置显示，并限制在可视窗口内，不能溢出屏幕。

## 6. 不向后台开放的服务器保护项

以下参数属于数据安全和资源保护边界，继续由程序固定控制，不允许管理员在 WebUI 中随意调整：

- 文件 I/O 并发数。
- WebDAV 总并发数。
- Argon2 密码验证并发数。
- 打包下载并发数。
- 文件事务全局锁。
- 磁盘安全预留空间。

它们仍应在代码和架构文档中有明确默认值、上限和测试，但不是普通业务设置。

## 7. 配置与验收要求

- 新设置写入版本化配置并采用原子持久化；迁移前保留有效备份。
- 旧配置升级后使用上述默认值，不能丢失账户、密码哈希、共享、文件锁或现有用户文件。
- 后端必须独立校验所有范围，不能只依赖前端输入限制。
- 登录日志接口必须验证管理员会话并限制查询范围和返回条数。
- 速率限制需要覆盖网页上传、普通下载、打包下载及 WebDAV 的回归测试。
- 框选需要覆盖无文本选择、滚动页面、双击、右键、外部文件拖入不上传及手机端禁用等测试。
- 右键菜单需要覆盖文件行、已选文件、列表空白、浏览框外部和访客权限五类场景。
- 完成后执行格式检查、全部 Rust 测试、严格 Clippy、Release 构建、JavaScript 语法检查和桌面/手机浏览器冒烟测试。
