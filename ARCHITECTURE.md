# Ycloud 架构

## 技术栈

- 后端：Rust 2021、Tokio、Axum 0.8。
- 前端：Vue 3.5、TypeScript、Pinia、Vue Router、Vite。
- 存储：本地文件系统、S3 SigV4 兼容对象存储。

## 请求链路

```text
浏览器 / WebDAV
  -> Axum 路由
  -> 认证、授权、限流、代理与来源校验
  -> StorageRegistry（storage_id）
  -> 本地事务存储 / S3 存储
```

核心模块：

- `app.rs`：路由与中间件。
- `config.rs`：schema v9、校验、迁移和原子保存。
- `auth*.rs`、`security*.rs`：会话、密码、IP、限流和代理边界。
- `files.rs`、`file_access.rs`：文件 API 与权限。
- `storage_backend.rs`：多存储注册与统一接口。
- `storage*.rs`：本地路径、事务和流式 I/O。
- `s3*.rs`：S3 签名、分页、重试和对象操作。
- `webdav.rs`、`archive.rs`：WebDAV 与流式归档。

## 权限

- 网页访客：只读访问允许公开的存储，受网页密码和文件夹锁限制。
- 管理员：后台和全部文件权限。
- 普通账号：不能进入后台；管理员按存储授予浏览、下载及六项写权限。
- WebDAV：每个挂载使用独立凭据、根路径和只读状态。

所有权限由后端验证，前端隐藏按钮不构成安全边界。

## 存储边界

- 存储实例以不可变 `storage_id` 隔离，文件路径不包含存储名称。
- 本地路径只能来自部署声明的挂载；拒绝符号链接和 Windows 重解析点。
- 本地上传、替换、复制和删除经过 `.ycloud-system` 事务区。
- S3 凭据加密保存且不回显；端点、区域、寻址方式和私网访问必须校验。
- S3 移动不是原子操作，必须先复制成功再删除源对象。
- 大文件、下载和 ZIP 均流式处理，不整体载入内存。

默认限制：上传 5 GiB；ZIP 3 GiB/1000 项；磁盘保留 512 MiB；本地阻塞 I/O 并发 4；归档任务并发 1。硬边界由后端控制，不交给后台任意调整。

## 部署边界

- 默认监听 `127.0.0.1:18473`。
- 非回环监听必须明确选择可信局域网模式或可信 HTTPS 反向代理模式。
- 代理头只信任 `TRUSTED_PROXY_IPS` 中的连接来源。
- Ycloud 不内置 TLS。
- 运行配置、日志、凭据、用户文件、`target/` 和 `node_modules/` 不进入 Git。
