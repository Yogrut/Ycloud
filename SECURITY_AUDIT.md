# Ycloud 安全审核方案与执行记录

> 文档性质：安全审核基线、执行清单和证据模板。它不是安全认证，也不代表项目已经通过审核。任何“通过”结论都必须绑定具体源码提交、构建产物、部署配置和可复核证据。

| 项目 | 内容 |
| --- | --- |
| 文档版本 | 1.0 |
| 编制日期 | 2026-08-29 |
| 当前参考分支与提交 | `main` / `0961e39578f8fc8c6a68549234934da2a4c40694` |
| 当前工作区状态 | 存在未提交改动，仅用于准备审核，不可作为正式审核结论基线 |
| 正式审核提交 | 待冻结：`________________` |
| 构建产物 SHA-256 | 待填写：`________________` |
| 审核负责人 | 待填写 |
| 复核人 | 待填写 |

## 1. 审核目标

本次审核要回答以下问题：

1. 未登录访问者、普通账号、网页访客和 WebDAV 账号能否越过各自权限边界。
2. 管理员密码、TOTP 密钥、恢复码、会话令牌、S3 凭据和用户文件是否得到与其敏感度相匹配的保护。
3. 所有本地和对象存储文件操作是否始终被限制在指定 `storage_id`、存储根和授权路径内。
4. 上传、下载、批量操作、打包下载、目录遍历、WebDAV 和 S3 请求是否能抵抗资源耗尽、状态竞争和失败恢复问题。
5. HTTP、反向代理、Cookie、CSRF、浏览器预览和安全响应头是否构成闭合的 Web 安全边界。
6. 默认配置是否安全，危险部署模式是否必须显式开启并能被清楚识别。
7. 依赖、构建、发布、备份、恢复和日志是否具备可追溯性。

审核以机密性、完整性、可用性和可追责性为共同目标。安全结论不能只依据前端是否隐藏按钮，也不能只依据设计文档；后端执行路径、自动化测试和隔离环境中的动态验证缺一不可。

## 2. 范围与责任边界

### 2.1 纳入范围

- Rust/Axum 后端及其所有公开路由、中间件、后台任务和错误处理。
- Vue/TypeScript 前端的登录、管理、文件浏览、上传、预览和凭据展示逻辑。
- 管理员、普通账号、网页访问密码、文件夹锁和独立 WebDAV 凭据。
- 本地存储、S3 兼容存储、存储切换、容量限制、事务恢复和测试连接。
- 配置持久化、主密钥、S3 凭据、TOTP 密钥、恢复码和初始凭据生命周期。
- 上传、下载、Range、批量操作、ZIP 打包、WebDAV 方法和目录分页。
- 监听地址、局域网明文模式、HTTPS 反向代理、可信代理和安全 Cookie。
- Cargo/npm 依赖、锁文件、CI、第三方公告例外、发布产物和运行文件权限。
- 登录安全状态、访问日志、错误日志、备份和恢复流程。

### 2.2 不直接替第三方负责的部分

以下内容不属于 Ycloud 自身实现漏洞，但其接口和错误配置影响必须纳入部署审核：

| 责任方 | 主要责任 | Ycloud 仍需承担的责任 |
| --- | --- | --- |
| S3/OSS/COS/MinIO 提供方 | 账号体系、Bucket 策略、服务端加密、基础设施和可用性 | 只请求已声明的能力；正确签名；不泄露凭据；安全校验 Endpoint；失败时不扩大破坏范围 |
| 部署者 | 主机补丁、服务账号、磁盘权限、TLS 证书、反向代理、防火墙、备份介质 | 默认配置应安全；危险选项需显式启用；启动时拒绝不一致的公网配置；提供可验证的部署说明 |
| 浏览器/WebDAV 客户端 | 客户端设备和本地凭据安全 | 不在响应、日志或前端存储中泄露秘密；Cookie 和 Basic Auth 的传输边界必须明确 |
| 审核人员 | 隔离环境、测试授权、证据保管和负载上限 | 项目应提供可重复的测试入口、明确错误和可回滚测试存储 |

S3 凭据最小权限属于第三方账号的部署策略，不是 Ycloud 能单方面保证的安全属性；但它仍是降低凭据泄露后影响范围的重要纵深防御。审核应确认 Ycloud 文档列出的必要操作与实际调用一致，不要求无关的管理权限。

### 2.3 明确不做的事情

- 不在生产存储上执行删除、覆盖、海量上传或故障注入。
- 不把真实管理员密码、TOTP 种子、恢复码、Session、S3 Key、WebDAV 密码或用户文件写入审核报告。
- 不以扫描器“未发现问题”代替人工授权、路径和状态机审核。
- 不自动执行 `npm audit fix`、无审查升级依赖或改变生产配置。

## 3. 系统、资产与信任边界

### 3.1 请求和数据流

```text
浏览器 ──HTTPS──> 可信反向代理 ──受控代理头──> Ycloud/Axum
   │                                              │
   └────────── 回环或显式局域网模式 ──────────────┤
                                                  ├── 本地存储根
WebDAV 客户端 ───── Basic Auth ───────────────────┤
                                                  ├── 允许的 S3 Endpoint
管理员配置 ─────── 后端权限检查 ─────────────────┤
                                                  └── 配置、主密钥和安全日志
```

### 3.2 关键资产

| 资产 | 期望属性 | 主要威胁 |
| --- | --- | --- |
| 管理员和普通账号密码哈希 | 不可逆、带随机盐、成本足够、不可枚举账号 | 离线破解、在线爆破、计时差异 |
| 管理员 TOTP 种子与恢复码 | 种子保密；恢复码单次使用且仅存哈希 | 配置泄露、重放、日志或 UI 泄露 |
| Session、网页访问和文件夹令牌 | 高熵、最小作用域、有期限、可撤销 | 固定会话、窃取、跨主体复用、权限变更后继续有效 |
| S3 Access Key/Secret Key | 静态和传输中保密、只送往批准的 Endpoint | 配置泄露、日志泄露、SSRF、错误重定向 |
| 主密钥 | 可恢复但不能随普通配置副本无保护泄露 | 丢失导致不可解密；同机权限突破后被读取 |
| 用户文件和元数据 | 仅授权主体可读写；操作保持一致性 | 越权、目录逃逸、竞态、覆盖、部分提交 |
| 配置、容量账本和事务记录 | 原子、可恢复、权限受限、格式可验证 | 崩溃损坏、回滚、伪造、并发更新丢失 |
| 安全日志 | 可追踪、限量、无秘密、难以伪造 | 注入、删改、磁盘耗尽、隐私泄露 |
| 构建与依赖 | 来源可追溯、版本锁定、公告持续复核 | 依赖投毒、过期例外、构建环境漂移 |

### 3.3 攻击者模型

- 可访问公网入口但未认证的远程攻击者。
- 可访问同一局域网、能观察或修改明文 HTTP 流量的设备。
- 能诱导浏览器访问攻击者域名并改变其 DNS 解析、尝试借 Host/DNS Rebinding 触达回环或 LAN 服务的一方。
- 已登录但只拥有部分存储权限的普通账号。
- 只获得网页访问密码、文件夹解锁令牌或某个 WebDAV 挂载凭据的访问者。
- 能上传任意名称和内容、并诱导管理员预览文件的恶意用户。
- 控制自定义 S3 服务、DNS 或反向代理配置的一方。
- 与 Ycloud 使用不同系统账号、但能尝试读取运行目录的本机低权限用户。
- 能提交依赖更新、构建配置或前端资源的供应链攻击者。
- 无恶意但可能误配监听地址、Cookie、代理、主密钥或存储路径的管理员。

宿主机 root/Administrator 或 Ycloud 运行账号完全失陷后，应用层加密不能保证秘密仍不可读；此场景用于界定影响和恢复，不作不现实的防护承诺。

## 4. 风险与证据规则

### 4.1 严重度

严重度以“可利用性 × 影响”评估，并结合是否默认暴露、是否需要管理员参与及影响范围修正：

| 等级 | 典型条件 | 发布处理 |
| --- | --- | --- |
| 严重 Critical | 未认证远程代码执行；管理员/S3 明文秘密直接泄露；无交互跨存储任意读写；主密钥体系整体失效 | 立即阻断发布或下线受影响能力 |
| 高 High | 认证绕过、稳定权限提升、目录逃逸、可实际利用的同源持久 XSS、大范围数据破坏或公网默认不安全 | 公网发布前必须修复并复测 |
| 中 Medium | 需要特定权限或误配的越权；有限信息泄露；可恢复 DoS；缺少关键纵深防御 | 指定负责人和期限；未修复需书面接受 |
| 低 Low | 影响有限、利用困难的加固项、审计性或文档缺口 | 纳入维护计划 |
| 信息 Info | 没有直接风险的观察或改进建议 | 可选优化 |

### 4.2 状态

使用以下状态，避免把待办误写成结论：

- `待审核`：尚未取得足够证据。
- `待动态验证`：代码路径已审阅，但运行行为未验证。
- `已确认`：能稳定复现且影响明确。
- `无法复现`：按记录环境与步骤未复现，不等于永久安全。
- `已修复待复测`：代码已改但尚未独立复测。
- `已关闭`：修复、测试和复核证据完整。
- `接受风险`：有负责人、理由、补偿控制和到期日。

### 4.3 证据要求

每个结论至少记录：审核基线、环境、前置权限、请求或操作摘要、预期、实际、服务端日志关联 ID、截图/测试输出、影响判断和复测结果。证据必须脱敏；请求头中的 `Authorization`、`Cookie`、恢复码和对象存储凭据只记录“已设置/已省略”，不得记录值。

## 5. 正式审核前的基线冻结

- [ ] 停止在审核分支继续混入功能和 UI 改动。
- [ ] 记录 `git rev-parse HEAD`、分支、Tag、`git status --short` 和 `git diff --check`。
- [ ] 确认工作树干净；若必须审核未提交树，保存受控补丁哈希并说明原因。
- [ ] 保存 `Cargo.lock`、`frontend/package-lock.json` 和工具链版本。
- [ ] 对待发布后端可执行文件、静态资源包、容器镜像记录 SHA-256 或镜像 Digest。
- [ ] 为本地存储准备一次性目录；为 S3 准备一次性账号、Bucket 和 Prefix。
- [ ] 使用专门的测试管理员、普通账号、WebDAV 挂载和受限存储，不复用生产凭据。
- [ ] 保存一份脱敏部署清单：监听地址、TLS 终止点、可信代理、Cookie 模式、存储类型和主密钥来源。
- [ ] 明确允许的压力上限、故障注入范围、恢复点和停止条件。
- [ ] 校准测试机和服务器时间，TOTP 测试同时记录时间源。

正式结论必须引用冻结后的提交；本文件顶部的提交只是编制文档时的参考 HEAD，因为当前工作区存在大量未提交改动。

### 5.1 最小环境矩阵

正式审核不能只在开发机和单一后端上运行。至少覆盖下表，无法覆盖的组合要记录原因和剩余风险：

| 维度 | 必测环境 |
| --- | --- |
| 操作系统/文件系统 | Windows + NTFS；Linux + 常见本地文件系统；实际使用的容器卷或网络盘另列 |
| 网络模式 | 回环直连；显式可信 LAN；公网 HTTPS 反向代理；禁止的错误组合 |
| 本地存储 | 默认存储根；预声明额外挂载；只读/权限错误/磁盘满/外部进程改动 |
| 对象存储 | 阿里 OSS、腾讯 COS、MinIO/RustFS、目标通用 S3；每类使用隔离 Bucket/Prefix |
| 身份 | 匿名、网页访客、管理员、启用/禁用普通账号、只读/部分/完整权限账号、WebDAV 挂载 |
| 2FA | 未启用、已启用、正常 TOTP、时钟偏差、恢复码、恢复码耗尽 |
| 浏览器/客户端 | Chromium 系、Firefox；实际支持的 WebDAV 客户端；移动端只验证安全行为差异 |
| 状态 | 全新安装、旧配置迁移、凭据轮换、权限撤销、异常退出后重启、备份恢复 |

## 6. 当前代码中的安全控制基线

下表只是代码证据索引，所有条目仍需在冻结基线上重新验证。

| 控制 | 代码证据 | 当前证据级别 | 动态审核重点 |
| --- | --- | --- | --- |
| 默认回环监听；非回环模式显式校验 | `src/config/runtime.rs` | 已实现 | 错误组合能否启动；代理与局域网边界 |
| 可信代理、Host、Proto 和单值转发头校验 | `src/security.rs::proxy_boundary_middleware` | 已实现并有单元测试 | 多层代理、IPv4/IPv6、重复头和直连绕过 |
| HttpOnly、SameSite=Strict、可配置 Secure Cookie | `src/security.rs::session_cookie` | 已实现并有单元测试 | 公网所有令牌是否都带 Secure；注销清除一致性 |
| Cookie 认证的同源/Origin CSRF 校验 | `src/security.rs::csrf_middleware` | 已实现 | 无 Origin、旧客户端、代理重写和各写路由覆盖 |
| CSP、DENY frame、nosniff、Referrer/Permissions Policy、公开模式 HSTS | `src/security.rs::security_headers_middleware` | 已实现 | 所有错误、流式和静态响应是否覆盖 |
| 管理员、普通账号、网页访问和 WebDAV 边界 | `src/auth.rs`、`src/file_access.rs`、`src/webdav.rs` | 已实现 | 每个对象级操作和权限撤销 |
| Argon2 密码哈希、未知账号仍执行哈希验证 | `src/config/password.rs`、`src/auth.rs::login_handler` | 已实现 | 参数基线、并发上限、计时和拒绝服务 |
| 管理员 TOTP、二维码、哈希恢复码和启停后会话处理 | `src/totp.rs`、`src/admin_api.rs` | 已实现 | 重放、并发使用恢复码、时钟偏差、秘密生命周期 |
| 会话和访问令牌的总量/单主体上限 | `src/auth.rs` | 已实现并有单元测试 | 权限变化、集群、TTL、窃取和轮换 |
| 路由 Body 上限、常规/上传超时和敏感头日志标记 | `src/app.rs` | 已实现 | chunked 请求、慢客户端、代理限制是否一致 |
| 本地路径规范化、拒绝 `..`、反斜线、冒号、系统目录和链接/重解析点 | `src/storage/path.rs` | 已实现并有单元测试 | 竞态、硬链接、挂载点、Unicode 和 Windows 名称 |
| 原子写入、事务区、恢复和磁盘保留 | `src/storage/`、`src/storage_transaction.rs`、`src/capacity.rs` | 已实现 | 崩溃点、磁盘满、断电、并发覆盖 |
| 上传批次大小/条目/票据限制 | `src/upload_batch.rs` | 已实现并有单元测试 | 票据主体绑定、并发、取消、过期和权限撤销 |
| ZIP 大小/条目/并发/票据/空闲超时和源文件变更检查 | `src/archive.rs` | 已实现并有单元测试 | 票据主体绑定、授权复查、客户端断开和名称安全 |
| S3 官方 Endpoint 约束和自建 Endpoint 精确允许列表 | `src/config/validation.rs`、`src/config/runtime.rs` | 已实现 | DNS、重定向、私网地址、TLS 和凭据目标 |
| S3/TOTP 敏感字段 AES-256-GCM 加密，密文绑定上下文 | `src/config/secret_store.rs`、`src/config/persistence.rs` | 已实现并有单元测试 | 主密钥备份、迁移、轮换、文件 ACL 和故障恢复 |
| Windows DPAPI；Unix 主密钥/配置/日志权限收紧 | `src/config/secret_store.rs`、`src/config/persistence.rs`、`src/login_security.rs` | 部分平台实现 | 实际服务账号、ACL、容器卷、备份恢复 |
| 登录失败限制、人工封禁、事件保留和有界记录 | `src/login_security.rs` | 已实现 | 多实例一致性、NAT 误伤、日志完整性和磁盘故障 |
| 锁文件、Cargo Deny 规则、npm CI 和 npm Dependabot | `Cargo.lock`、`deny.toml`、`frontend/package-lock.json`、`.github/` | 部分已配置 | Rust CI/Cargo 更新是否完整、例外是否过期、构建来源 |

### 6.1 当前路由安全层映射

下表是静态路由映射，帮助检查中间件是否遗漏；它不替代逐路由负向测试。

| 路由组 | 基础认证 | 动作/对象授权 | CSRF/其他边界 |
| --- | --- | --- | --- |
| `/api/login`、`/api/gate` | 自身凭据认证 | 不适用 | 位于 CSRF 层内；无既有 Cookie 时不触发 Cookie CSRF；JSON 跨源仍受预检和无 CORS 限制 |
| `/api/logout` | 读取现有 Cookie | 撤销对应令牌 | Cookie 写请求受 CSRF |
| `/api/me` | 可匿名返回当前身份状态 | 不适用 | GET；敏感缓存策略需审核 |
| `/api/folder/unlock` | Admin/User/Gate 基础身份 | 文件夹密码与锁作用域 | Cookie CSRF；另有固定窗口入口限流 |
| `/api` 读取组 | Admin/User/Gate | Browse/Download、存储和文件夹锁 | GET/HEAD；下载/预览响应策略另审 |
| `/api` 写入组 | Admin/User | 每动作存储权限、可写状态、文件夹锁 | Cookie CSRF；Body/批量/上传限制 |
| `/api/admin/*` | 仅 Administrator | 管理员操作 | Cookie CSRF；64 KiB 默认管理 Body 上限 |
| `/dav/*` | 独立 WebDAV Basic | 挂载存储、路径和只读/可写方法 | 不使用 Cookie，故不走 CSRF；Body、并发和密码验证边界另审 |
| `/api/health`、`/api/ready` | 无 | 只返回存活/就绪 | 不得泄露配置、路径或第三方错误 |
| 静态页面/资源 | 无 | 无 | 全局安全响应头和缓存策略 |

## 7. 全面审核矩阵

优先级含义：`P0` 为发布阻断面，`P1` 为必须完成的主要审核，`P2` 为加固和运维完整性。

### 7.1 构建、配置和暴露面

| ID | 优先级 | 审核内容 | 通过标准 |
| --- | --- | --- | --- |
| BASE-01 | P0 | 冻结源码、锁文件、静态资源和二进制的同一基线 | 提交、产物哈希和测试报告可互相对应 |
| BASE-02 | P0 | 枚举所有 HTTP 路由、WebDAV 方法、后台任务和出站目标 | 没有未记录或绕开通用中间件的入口 |
| BASE-03 | P1 | 比较 `config.example.json`、环境变量默认值和实际运行值 | 示例不包含秘密；默认只回环；危险模式显式开启 |
| BASE-04 | P1 | 检查运行文件是否被 Git 忽略且历史中从未提交 | 配置、日志、主密钥、初始凭据和用户文件无泄露 |
| BASE-05 | P1 | 审核错误页、健康/就绪探针和静态资源 | 不泄露路径、配置、依赖版本或内部错误；探针不需秘密且不暴露敏感状态 |
| BASE-06 | P2 | 审核 Debug/Release 差异、panic、backtrace 和日志等级 | Release 不启用危险调试；panic 不返回内部细节 |

### 7.2 身份认证、密码和两步验证

| ID | 优先级 | 审核内容 | 通过标准 |
| --- | --- | --- | --- |
| AUTH-01 | P0 | 管理员、普通账号、网页密码、文件夹锁和 WebDAV 凭据入口逐一验证 | 每类凭据只能获得设计中的主体和作用域 |
| AUTH-02 | P0 | 对不存在账号、错误密码、禁用账号和错误二次验证码比较响应与耗时 | 不泄露账号是否存在；错误内容稳定；耗时无明显低成本分支 |
| AUTH-03 | P0 | 管理员未启用 TOTP 与已启用 TOTP 两种登录流程 | 未启用时不请求验证码；启用时密码正确后才进入二次验证，不可跳过 |
| AUTH-04 | P0 | TOTP 设置、确认、重复设置、禁用和管理员密码变更 | 均要求当前高可信认证；状态变更后旧管理员 Session 失效 |
| AUTH-05 | P1 | TOTP 时间窗口、服务器时钟偏差和同时间步重放 | 窗口有明确边界；是否阻止同时间步重放有安全决定和测试 |
| AUTH-06 | P0 | 恢复码展示、存储、并发使用和重复使用 | 只在生成时展示一次；只存 Argon2 哈希；成功使用后原子删除；不可重放 |
| AUTH-07 | P1 | Argon2 算法版本、内存/时间/并行参数、盐和密码长度 | 参数在目标 VPS 上足够安全且不会轻易造成验证队列 DoS；每个哈希有独立随机盐 |
| AUTH-08 | P1 | 登录限流、持久封禁和密码验证并发队列 | IP 与入口策略按设计生效；重启后持久；容量耗尽时安全失败；不会绕过或无限占用 CPU |
| AUTH-09 | P1 | 管理员改名、改密、删除/禁用普通账号后的会话处理 | 相关会话立即撤销；其他无关主体不受影响 |
| AUTH-10 | P2 | 初始管理员凭据的生成、首次展示、删除和恢复 | 随机、权限受限、改密后删除；不会在日志或重复 API 中返回 |

### 7.3 会话、令牌和 CSRF

| ID | 优先级 | 审核内容 | 通过标准 |
| --- | --- | --- | --- |
| SESS-01 | P0 | Session、网页访问和文件夹令牌的随机性、名称、TTL 和作用域 | 使用密码学安全随机值；不同类型不可互换；过期后立即拒绝 |
| SESS-02 | P0 | Cookie 的 `HttpOnly`、`SameSite`、`Secure`、`Path` 和清除属性 | 公网全部带 Secure；清除 Cookie 的作用域与创建完全一致 |
| SESS-03 | P0 | Cookie 认证写请求的 CSRF 覆盖 | 所有写 API 均要求同源 Fetch Metadata 或精确 Origin；缺失/多值/畸形头安全拒绝 |
| SESS-04 | P1 | 登录前后令牌轮换、会话固定、注销和并发 Session 上限 | 登录产生新令牌；注销只撤销当前令牌；上限行为可预测且有测试 |
| SESS-05 | P1 | 绝对 TTL、空闲 TTL 和高风险操作重新认证 | 策略被明确记录；改凭据、启停 2FA 等操作不能只依赖长期旧会话 |
| SESS-06 | P0 | 用户/存储/权限禁用后，已有 Session、上传票据和归档票据 | 后续每次敏感操作重新查权限，不能靠旧票据维持已撤销能力 |
| SESS-07 | P1 | 多实例部署下 Session、限流、封禁和一次性恢复码一致性 | 若不支持集群必须启动或文档明确限制；若支持则状态共享且原子 |
| SESS-08 | P2 | 浏览器缓存、Back/Forward Cache 和敏感页面缓存头 | 登出后敏感管理响应不可从缓存恢复；秘密响应使用 `no-store` |

### 7.4 授权和对象级访问控制

| ID | 优先级 | 审核内容 | 通过标准 |
| --- | --- | --- | --- |
| ACL-01 | P0 | `/api/admin/*` 每个路由的管理员认证 | 普通账号、访客、WebDAV 凭据和匿名请求均被拒绝 |
| ACL-02 | P0 | 每个文件 API 的 `storage_id` 与 Browse/Download/Upload/Create/Rename/Move/Copy/Delete 权限 | 后端逐操作判断；直接构造请求不能绕过 UI |
| ACL-03 | P0 | 批量删除、复制和移动的源、目标、每一项及部分失败 | 源和目标都检查相应权限；无越权的部分提交；结果逐项可追踪 |
| ACL-04 | P0 | 默认存储、访客存储切换和禁用存储 | 不因缺失或伪造 `storage_id` 回退到更高权限存储 |
| ACL-05 | P0 | 普通账号枚举不可见存储、用户、锁、挂载和后台配置 | 返回不存在或拒绝，不泄露凭据和敏感元数据 |
| ACL-06 | P1 | 文件夹锁对列表、预览、下载、上传、批量和打包路径 | 所有进入或影响受锁路径的操作一致验证；父子锁选择正确 |
| ACL-07 | P1 | 文件夹解锁令牌作用域、密码变化和锁删除 | 令牌只适用于对应锁；密码变化立即撤销；不能跨存储复用 |
| ACL-08 | P1 | 存储名称变化与不可变 `storage_id` | 权限和路径不依赖可变显示名；旧 ID 不被错误复用 |
| ACL-09 | P1 | 管理员“测试连接”“暂存”“激活”“删除配置” | 每一步只由管理员执行；测试不隐式激活；删除配置不删除用户数据 |
| ACL-10 | P0 | 不同 `storage_id` 是否映射到相同/嵌套物理命名空间 | 本地挂载按 canonical/平台文件标识去重且不嵌套；同 S3 Origin+Bucket 的 Prefix 不重叠；若产品允许共享命名空间，必须进入同一授权域并显式告警 |

### 7.5 HTTP、代理与浏览器边界

| ID | 优先级 | 审核内容 | 通过标准 |
| --- | --- | --- | --- |
| HTTP-01 | P0 | 回环、局域网明文和公开 HTTPS 三种启动矩阵 | 不安全组合拒绝启动；公开模式要求 Secure Cookie、HTTPS Origin 和精确可信代理 IP |
| HTTP-02 | P0 | 直连应用端口和伪造 `X-Forwarded-*`、Host、重复头 | 只有可信代理来源的单值头被接受；其他来源不能伪造客户端 IP/协议/主机 |
| HTTP-03 | P1 | 反向代理的请求体、超时、缓冲和连接限制 | 代理限制不弱于应用；大文件流式上传不会被意外完整缓冲 |
| HTTP-04 | P0 | CSP、frame、nosniff、Referrer、Permissions Policy、COOP 和 HSTS 覆盖 | 静态、API、错误、预览、下载和流式响应均得到适合的头；HSTS 仅在 HTTPS 公网模式发送 |
| HTTP-05 | P1 | CORS、OPTIONS 和不支持的方法 | 默认不允许跨源带凭据；方法白名单明确；错误不回显敏感输入 |
| HTTP-06 | P1 | Content-Length、Transfer-Encoding、重复长度头和代理解析差异 | 前后端解析一致；畸形请求在进入业务前拒绝 |
| HTTP-07 | P1 | 常规、上传、WebDAV、批量和管理请求体上限 | 每条路由均有合适上限；JSON 元数据不能借用大上传上限分配内存 |
| HTTP-08 | P2 | 请求 ID、敏感头标记和客户端可控头 | 服务端生成不可由客户端覆盖或污染；日志关联但不泄露秘密 |
| HTTP-09 | P0 | 回环/LAN 模式的 Host、Origin 与 DNS Rebinding | 只接受明确配置或监听对应的 Host/Origin；攻击者域名解析到回环/LAN 后不能获得自动 Gate、读取文件或完成 Cookie 写请求 |

### 7.6 本地路径、文件名和事务安全

| ID | 优先级 | 审核内容 | 通过标准 |
| --- | --- | --- | --- |
| PATH-01 | P0 | 绝对路径、父级组件、空组件、点组件、反斜线、NUL、冒号和保留系统目录 | 所有入口统一规范化并在访问前拒绝危险路径 |
| PATH-02 | P0 | URL 百分号解码、重复编码、UTF-8 异常和 WebDAV 路径 | 只按预期解码一次；路由层和存储层理解一致；非法编码安全拒绝 |
| PATH-03 | P1 | Unicode 正规化、大小写碰撞、尾随点/空格、Windows 设备名和 ADS | 各支持平台行为被测试；不会映射到意外文件或覆盖不同名称 |
| PATH-04 | P0 | 符号链接、Windows Junction/重解析点和链接到根外 | 创建前、打开前及递归遍历时均拒绝；不能借已有链接逃逸 |
| PATH-05 | P0 | 检查与使用之间替换目录/链接的 TOCTOU | 高风险操作使用安全句柄/原子策略或有可证明的竞态防护 |
| PATH-06 | P1 | 硬链接、挂载点、网络共享和跨卷重命名 | 支持边界明确；不能借硬链接访问未授权数据；跨卷失败可恢复 |
| PATH-07 | P0 | `.ycloud-system`、事务临时名、容量账本和配置目录 | 浏览、WebDAV、S3 Key 和任何变体都不可访问或覆盖内部对象 |
| PATH-08 | P1 | 重命名/复制/移动到自身、子目录、已存在目标和仅大小写变化 | 冲突语义一致；无无限递归、丢失或越界 |
| PATH-09 | P1 | 崩溃、磁盘满、权限拒绝和进程终止下的原子写/删除/恢复 | 原文件或新文件至多一个可见；临时对象可恢复或安全清理 |

### 7.7 上传、下载、预览、批量和归档

| ID | 优先级 | 审核内容 | 通过标准 |
| --- | --- | --- | --- |
| FILE-01 | P0 | 单文件上限、批次总字节、批次条目和部署绝对上限 | UI 配置不能突破部署上限；实际流量超过声明时中止且不提交 |
| FILE-02 | P0 | 有/无 Content-Length、chunked、提前结束、超量和慢速请求 | 各模式安全处理；批次强制声明长度；临时文件和容量预留被释放 |
| FILE-03 | P0 | 批次票据的熵、TTL、单次状态、路径/大小/存储/发起主体绑定 | 票据不能跨主体、跨会话、跨存储或改路径复用；完成/取消/过期后失效 |
| FILE-04 | P1 | 暂停、继续、终止、失败重试和清除任务记录 | 前端操作不会误删已上传文件；重试不能重复提交已成功项 |
| FILE-05 | P1 | 覆盖已存在文件、并发上传同一路径和取消时机 | 覆盖策略明确；最终内容完整；容量账本和事务状态一致 |
| FILE-06 | P0 | 下载/预览/归档在创建响应前和流式期间的授权 | 权限与文件夹锁至少在开始时重新确认；撤权策略有明确取舍 |
| FILE-07 | P1 | Range 的单范围、后缀范围、越界、多范围和空文件 | 状态、Content-Range/Length 正确；不越界、不整数溢出、不绕过限速 |
| FILE-08 | P0 | HTML、SVG、XML、脚本、PDF 和伪造扩展名的同源预览 | 主动内容不在拥有 Ycloud 权限的同源上下文执行；不安全类型强制纯文本/下载或隔离域沙箱 |
| FILE-09 | P1 | `Content-Type`、`Content-Disposition`、UTF-8 文件名和 CR/LF | 不信任上传 Content-Type；响应头不可注入；下载名兼容且安全 |
| FILE-10 | P0 | 归档票据的主体/Session/存储绑定、单次消费和权限变化 | 泄露票据不能被另一主体使用；消费后不可重放；撤权后安全拒绝 |
| FILE-11 | P1 | ZIP 条目名称、重复选择、父子选择和源文件变化 | 条目始终为安全相对名；去重正确；源变更中止而不生成误导内容 |
| FILE-12 | P1 | 归档条目/字节/并发/待处理票据/空闲超时和客户端断开 | 所有资源有界；断开后任务停止；许可证/内存/临时空间无泄漏 |
| FILE-13 | P1 | 目录搜索、排序、分页游标和超大目录 | 游标绑定存储、路径和查询；长度有界；不因目录规模线性占用内存 |

### 7.8 S3、OSS、COS 与自建对象存储

| ID | 优先级 | 审核内容 | 通过标准 |
| --- | --- | --- | --- |
| S3-01 | P0 | 阿里 OSS、腾讯 COS 的 Region、官方域名、HTTPS、端口和寻址方式 | 只允许与 Region 匹配的官方 HTTPS Origin，不能改为用户信息/路径/查询形式 |
| S3-02 | P0 | MinIO/RustFS/通用 S3 的 `S3_ALLOWED_ENDPOINTS` | 未在部署白名单中的 Origin 拒绝测试、暂存、激活和运行时连接 |
| S3-03 | P0 | 自定义 Endpoint 的 DNS 解析、私网/环回/链路本地/元数据地址与 IPv4/IPv6 | 私网 S3 是显式部署选择；地址变化、DNS 重绑定和特殊地址有明确策略及测试 |
| S3-04 | P0 | 3xx 重定向、代理、TLS 证书、SNI 和主机变化 | 凭据不会被发送到未批准的 Origin；TLS 验证不可静默关闭；重定向策略明确 |
| S3-05 | P0 | 凭据在 API、Debug、错误、日志、测试结果和配置响应中的表现 | 只显示“已配置”；任何响应和日志不含 Access/Secret、签名或完整敏感 URL |
| S3-06 | P1 | “测试连接”的临时对象命名、创建、读取、复制、删除及失败清理 | 使用随机且隔离的内部 Prefix；不触碰现有对象；每个失败点有清理和可审计结果 |
| S3-07 | P1 | Prefix、Bucket、对象 Key、目录标记和内部事务 Key 空间 | 用户路径不能进入内部 Prefix；相似前缀不串目录；异常 Key 被忽略或安全拒绝 |
| S3-08 | P1 | Multipart 上传和大对象 Copy 的初始化、分片、完成、Abort 与重试 | 失败/取消后主动 Abort；完成结果验证；重试幂等且不遗留可见半成品 |
| S3-09 | P1 | 移动“先复制后删除”、目录事务和失败恢复 | 不宣称原子；复制成功后才删除；每个中断点可恢复且不会误删源 |
| S3-10 | P1 | 列表分页、容量核对、远端慢响应和恶意服务响应 | 响应大小、页数、并发和时间有界；解析失败不耗尽内存；请求取消可传播 |
| S3-11 | P2 | S3 权限文档与实际 API 调用 | 文档只要求必要操作；拒绝权限时错误清晰；不要求无关管理权限 |

### 7.9 WebDAV

| ID | 优先级 | 审核内容 | 通过标准 |
| --- | --- | --- | --- |
| DAV-01 | P0 | Basic Auth 仅在 HTTPS 或明确接受风险的可信 LAN 使用 | 公网文档和启动策略不允许明文凭据；错误响应含正确 Challenge 但不泄露账号状态 |
| DAV-02 | P0 | 挂载凭据、启用状态、绑定存储路径和读写权限 | 每次请求重新读取有效配置；禁用/改密后旧凭据立即失效 |
| DAV-03 | P0 | GET/HEAD/PUT/DELETE/MKCOL/MOVE/COPY/PROPFIND 方法权限 | 每个方法映射正确 StorageAction；只读挂载不能以替代方法写入 |
| DAV-04 | P0 | 挂载根、编码路径、反斜线、点组件、保留目录和根删除 | 统一路径规范化；不能逃出挂载路径或删除挂载根 |
| DAV-05 | P0 | MOVE/COPY `Destination` 的 Host、路径、挂载名和同源规则 | 只接受精确当前 Host 与当前挂载；不能跨挂载或通过绝对 URI 绕过 |
| DAV-06 | P1 | `Overwrite`、目标存在、源等于目标和目录后代 | RFC 语义明确且不发生意外覆盖/递归 |
| DAV-07 | P1 | PROPFIND Depth、超大目录、XML 转义和响应内存 | Depth 有上限；目录条目有界；名称、URL 和属性均正确转义 |
| DAV-08 | P1 | XML 请求体、实体、超长属性和未实现方法 | 不解析外部实体；请求体有界；LOCK/UNLOCK/PROPPATCH 返回明确且无状态副作用 |
| DAV-09 | P1 | WebDAV 密码验证并发、IP 限制和常见客户端重试 | 错误凭据不能耗尽 Argon2 队列；成功日志节流不妨碍失败审计 |
| DAV-10 | P2 | Windows/macOS/Linux 常见客户端互操作 | 安全限制不因兼容性分支被关闭；差异均有测试记录 |

### 7.10 配置、主密钥和秘密生命周期

| ID | 优先级 | 审核内容 | 通过标准 |
| --- | --- | --- | --- |
| KEY-01 | P0 | S3 Secret、Access Key 和 TOTP 种子静态存储 | 敏感字段使用带随机 Nonce 的认证加密；密文绑定字段/存储上下文；篡改后拒绝启动或加载 |
| KEY-02 | P0 | 单机自动主密钥的生成和权限 | 使用 CSPRNG；原子创建；Windows 绑定预期服务账号的 DPAPI；Unix 目录 0700/文件 0600 |
| KEY-03 | P0 | `YCLOUD_CONFIG_KEY_FILE` 和 `YCLOUD_CONFIG_KEY` 优先级、格式和泄露面 | 文件优先且必须为普通小文件；环境变量仅作覆盖；错误不回显值；来源变化可诊断 |
| KEY-04 | P0 | 主密钥与配置的备份、恢复和灾难演练 | 两者作为同一恢复单元受控备份；在全新主机成功恢复；密钥丢失行为明确且不静默重置 |
| KEY-05 | P1 | 明文旧配置迁移、崩溃窗口和备份轮换 | 迁移后主配置与备份均无明文；中途中断不留下更宽权限副本 |
| KEY-06 | P1 | 主密钥轮换/重加密和服务账号迁移 | 有原子、可回滚流程；旧密钥保留到验证完成；DPAPI 身份变化有操作说明 |
| KEY-07 | P1 | 内存、Core Dump、Swap、诊断导出和前端表单 | 不在前端持久存储秘密；日志和 Debug 脱敏；部署可关闭或保护 Core Dump |
| KEY-08 | P1 | 配置 API、备份文件、初始凭据和临时文件权限 | 所有副本同等保护；API 只返回布尔“已配置”而非原值 |
| KEY-09 | P2 | 威胁说明的真实性 | 明确“静态加密不防已控制 Ycloud 运行账号/宿主机管理员”，不作超出边界的宣传 |

### 7.11 前端和同源内容

| ID | 优先级 | 审核内容 | 通过标准 |
| --- | --- | --- | --- |
| WEB-01 | P0 | 文件名、存储名、日志、错误和用户输入的 Vue 渲染 | 默认文本转义；无不必要 `v-html`/`innerHTML`；属性和 URL 上下文安全 |
| WEB-02 | P0 | 上传内容的预览页面、iframe、PDF、媒体和纯文本 | 不安全主动内容不能访问 Ycloud 同源权限；新窗口使用 `noopener`；iframe 有合适隔离 |
| WEB-03 | P1 | 前端是否在 localStorage/sessionStorage/URL 保存凭据或令牌 | 只允许主题、语言等非秘密；密码、TOTP、恢复码、S3 Key 和 Session 不落前端存储/URL |
| WEB-04 | P1 | 登录的 TOTP 条件显示和错误状态 | 仅服务器在正确密码后请求时显示；刷新/返回不会保留密码或二次验证码 |
| WEB-05 | P1 | 管理员表单的掩码值、未修改字段和浏览器自动填充 | 掩码不被当成新密码提交；旧秘密不会读回；字段 autocomplete 策略合适 |
| WEB-06 | P1 | 上传拖拽、文件夹相对路径和前端拒绝结果 | 客户端校验只是体验层；服务端独立验证；错误文件仍可见且可单独处理 |
| WEB-07 | P2 | 第三方前端资源和图标 | 生产资源本地打包；不从未批准 CDN 执行脚本；Source Map 发布策略明确 |

### 7.12 日志、错误、审计与隐私

| ID | 优先级 | 审核内容 | 通过标准 |
| --- | --- | --- | --- |
| LOG-01 | P0 | 全局搜索密码、Cookie、Authorization、TOTP、恢复码、S3 Key 和签名 URL 的日志路径 | 任何级别均不记录秘密；敏感请求/响应头被标记；第三方错误先脱敏 |
| LOG-02 | P1 | 内部错误和公开 JSON 错误的分离 | 客户端仅收到稳定错误码和安全消息；内部路径/SDK 细节只进受保护日志 |
| LOG-03 | P1 | IP、User-Agent、结果和时间字段的规范化 | 控制字符和长度受限；无法伪造多行日志；时间统一且可校准 |
| LOG-04 | P1 | 登录事件状态、追加、压缩、备份和磁盘满 | 写入原子或可恢复；记录数量有界；磁盘故障不会默认放行认证限制 |
| LOG-05 | P1 | 管理员查询、筛选、分页和手工封禁/解封 | 仅管理员可访问；分页有界；管理动作本身可审计 |
| LOG-06 | P2 | 保留期、隐私告知、备份和删除 | 与配置一致；超期数据真实删除；日志备份同样受保护 |
| LOG-07 | P2 | 审计日志抗篡改边界 | 明确本地管理员可修改普通文件日志；高要求部署可外送只追加日志系统 |

### 7.13 可用性、并发和失败恢复

| ID | 优先级 | 审核内容 | 通过标准 |
| --- | --- | --- | --- |
| DOS-01 | P0 | 每个入口的请求体、条目数、字符串长度、并发、队列和超时 | 所有不可信输入和昂贵任务都有硬上限；管理员不能把固定安全上限无限放大 |
| DOS-02 | P1 | Argon2、WebDAV、目录列表、归档、本地 I/O 和 S3 请求的独立并发 | 一个子系统拥塞不会无限扩散；等待有超时或有界队列 |
| DOS-03 | P1 | 慢上传、慢下载、断连和 backpressure | 不完整任务及时释放句柄、票据、Multipart、临时文件和容量预留 |
| DOS-04 | P1 | 容量账本不可信、磁盘余量不足和远端容量未知 | 有容量上限时未知状态安全暂停写入；恢复任务单实例且不会请求风暴 |
| DOS-05 | P1 | 恶意超大目录、S3 分页循环和游标篡改 | 内存与页大小相关；远端无进展/重复游标被检测；请求可取消 |
| DOS-06 | P1 | 进程崩溃、强制终止和启动恢复 | 事务恢复幂等；内部垃圾有界清理；失败不会删除已提交用户数据 |
| DOS-07 | P2 | 健康和就绪探针在存储故障下的行为 | 存活与就绪区分；编排器不会因单个慢存储造成重启风暴 |

### 7.14 依赖、供应链和发布

| ID | 优先级 | 审核内容 | 通过标准 |
| --- | --- | --- | --- |
| SUP-01 | P0 | `Cargo.lock`、`package-lock.json` 和 CI 安装方式 | Release 使用锁文件；Rust 使用 `--locked`，前端使用 `npm ci` |
| SUP-02 | P1 | RustSec、npm Advisory、Yanked、许可证和来源检查 | 报告留档；严重/高风险无未处理项；来源仅允许批准 Registry |
| SUP-03 | P1 | `RUSTSEC-2026-0253` 例外 | 每次升级重新证明受影响路径不可达；例外有负责人和移除条件，不能永久静默忽略 |
| SUP-04 | P1 | GitHub Actions 权限、Action 固定版本和不可信 PR | 最小权限；无秘密暴露给不可信 Fork；高保障发布使用完整提交 SHA 固定 Action |
| SUP-05 | P1 | Rust 与前端 CI 覆盖一致性 | 后端 fmt/clippy/test/deny/audit 与前端 lint/test/build/audit 都在受保护分支强制执行 |
| SUP-06 | P1 | 构建机、Node/Rust 版本和产物来源 | 工具版本记录；构建可重复；发布产物签名或至少提供校验和 |
| SUP-07 | P2 | SBOM 和第三方许可证 | 每个 Release 生成后端/前端 SBOM 与第三方声明，能反查受影响版本 |
| SUP-08 | P2 | 依赖升级和安全回滚 | 单独 PR、完整测试、数据格式兼容检查和已验证回滚路径 |

## 8. 必须执行的动态测试集

以下用例应在隔离环境执行。步骤只描述验证目标；具体测试数据存放在受控证据目录，不写入本仓库。

| 用例 | 级别 | 场景 | 期望结果与证据 |
| --- | --- | --- | --- |
| T-01 | P0 | 匿名、访客、普通账号分别访问所有管理 API | 全部拒绝；记录状态码、公开错误和服务端请求 ID |
| T-02 | P0 | 普通账号按八种存储操作权限执行允许与不允许动作 | 权限矩阵逐格一致；直接请求与 UI 行为相同 |
| T-03 | P0 | 修改 `storage_id`、路径和目标存储执行单项/批量操作 | 无 IDOR、跨存储读取或部分越权提交 |
| T-04 | P0 | Cookie 写请求缺少或伪造同源信息 | 拒绝；合法同源请求通过；WebDAV Basic 不被错误套用 Cookie CSRF 逻辑 |
| T-05 | P0 | 从非可信源直连公开端口并伪造代理头 | 拒绝；可信代理的规范单值头通过 |
| T-06 | P0 | 不存在账号、错误密码、禁用账号和正确密码的时间分布 | 无稳定账号枚举差异；限流/封禁按入口生效 |
| T-07 | P0 | 启用 2FA、使用 TOTP/恢复码、重复使用恢复码、禁用 2FA | 二次验证不可绕过；恢复码单次；状态变化撤销旧会话 |
| T-08 | P0 | 用户禁用、权限撤销、存储禁用后复用旧 Session 和票据 | 所有后续敏感操作拒绝；行为与文档一致 |
| T-09 | P0 | 本地存储对编码路径、平台特殊名称和保留目录进行操作 | 无根外访问；无内部文件暴露；错误稳定 |
| T-10 | P0 | 在允许范围内交换目录与符号链接/Junction，制造检查使用竞态 | 不能越出存储根；无未授权文件读写 |
| T-11 | P0 | 超限、少报长度、提前断开和慢速单/批上传 | 无半文件；临时文件、容量预留和票据被释放 |
| T-12 | P0 | 两个有同存储上传权限的不同账号交换批次票据 | 票据应绑定发起主体；若可复用，按能力票据风险评估并修正 |
| T-13 | P0 | 两个下载权限不同的主体交换归档票据并在撤权后使用 | 非发起主体和撤权主体均不能下载；票据单次且过期拒绝 |
| T-14 | P0 | 上传主动内容并通过预览、新窗口、PDF/媒体入口打开 | 不在 Ycloud 权限同源中执行脚本或发起带权限写操作 |
| T-15 | P1 | Range 边界、多范围、空文件和限速组合 | 响应头正确；无越界、崩溃、绕过限速 |
| T-16 | P1 | 归档期间修改、删除、替换源文件并断开客户端 | 检测变化并中止；无无限后台任务或错误成功包 |
| T-17 | P0 | WebDAV 只读挂载尝试所有写方法；写挂载执行根和边界操作 | 方法权限准确；挂载根及根外内容不可修改 |
| T-18 | P0 | WebDAV MOVE/COPY 使用不同 Host、挂载和编码目标 | 非当前同源同挂载目标拒绝 |
| T-19 | P1 | PROPFIND Depth 与超大目录、特殊字符名称 | 响应有界并正确 XML 转义；不解析外部实体 |
| T-20 | P0 | 未允许的自建 S3 Origin、重定向、DNS 变化和 TLS 错误 | 不向未批准目标发送凭据；错误不泄密 |
| T-21 | P1 | S3 测试连接在创建/读取/复制/删除各阶段故障 | 不触碰既有对象；临时对象和 Multipart 可清理；失败可诊断 |
| T-22 | P1 | S3 上传、复制、移动在超时和进程终止后的恢复 | 无误删源；内部事务幂等恢复；用户可见状态明确 |
| T-23 | P0 | 篡改密文、换主密钥、丢失主密钥和恢复完整备份 | 篡改/错钥匙明确拒绝；不会生成新钥匙覆盖事实；完整备份可恢复 |
| T-24 | P1 | 从旧明文配置迁移并在关键写入点中断 | 最终主/备份无明文；失败可回滚；文件权限正确 |
| T-25 | P1 | 将控制字符和超长值放入 User-Agent、文件名和第三方错误 | 日志不换行伪造、不泄露秘密、长度有界 |
| T-26 | P1 | 同时施加登录、上传、下载、目录和 S3 慢响应负载 | 各资源上限生效；服务可恢复；无死锁或无界增长 |
| T-27 | P1 | 重启与多实例条件下验证 Session、封禁、票据和恢复码 | 与声明的部署模式一致；不支持的集群拓扑被明确阻止或警告 |
| T-28 | P0 | 在旧密码/旧锁密码异步验证期间执行改密、启用 2FA、禁用账号或修改锁，再释放验证请求 | 旧凭据不得在撤销动作完成后签发新的 Session/Gate/Unlock 令牌 |
| T-29 | P0 | 未解锁时对含锁后代的父目录执行删除、重命名、移动、复制，并测试 Windows 路径别名 | 全部安全拒绝，或在明确授权后事务性迁移锁；任何别名都不能绕过 |
| T-30 | P0 | S3 凭据轮换与旧明文配置迁移的每个保存点故障注入 | 主配置、备份和临时文件均不长期保留不应存在的旧/明文凭据 |
| T-31 | P0 | Windows 第二个低权限本机账号读取或改写初始凭据、配置、备份、主密钥和安全日志 | 显式 DACL 阻止读取/修改；服务身份仍可正常启动和轮换 |
| T-32 | P1 | 启用 Debug Trace 后执行带归档/上传票据、游标、敏感文件名的请求 | 日志只含路由模板和脱敏元数据，不含完整查询、票据或秘密 |
| T-33 | P1 | 执行账号、2FA、权限、S3、WebDAV、锁和安全策略变更 | 产生结构化管理员审计事件，含主体/IP/request ID/字段名/结果但无旧值、新值或秘密 |
| T-34 | P0 | 同一 IP 并发触发登录阈值，并让账号 A/WebDAV A 成功后继续攻击 B | Argon2 前原子限制；A 的成功不清除 B 的主体失败计数；全局 IP 桶仍限制扫描 |
| T-35 | P1 | 比较已知、未知、禁用 WebDAV 挂载及正确/错误用户名的状态和时间分布 | 外部响应符合统一枚举策略；错误用户名仍执行等成本虚拟验证 |
| T-36 | P0 | 应用回环监听、同机 HTTPS 代理并设置公网变量 | 仍进入明确代理模式，验证可信代理、真实 IP、Proto、Host、Secure Cookie/HSTS；冲突配置不被静默忽略 |
| T-37 | P1 | 只轮换管理员或网页门禁中的一种初始凭据 | 另一种默认凭据仍有效时持续高危提醒；两者均轮换后才完成首次安全配置 |
| T-38 | P1 | 顺序和并发重复提交同一有效 TOTP，并测试前后时间窗 | 同一计数器至多成功一次；允许的时钟容差不被破坏 |
| T-39 | P1 | 经实际反向代理检查 TOTP、恢复码、身份和管理员响应缓存 | 响应为 `no-store, private`；代理不缓存 `/api/*`；登出后不能从缓存恢复敏感内容 |
| T-40 | P0 | 配置路径别名/嵌套的两个本地存储，以及同 Origin+Bucket 的相同/重叠 S3 Prefix，并给予不同账号不同权限 | 系统拒绝形成重叠命名空间，或把它们作为同一授权域；不能经低权限 storage_id 访问高权限实例中的同一对象 |
| T-41 | P0 | 在回环和 LAN 模式中，经攻击者域名与 DNS 变化发送不同 Host/Origin 请求 | 非批准 Host/Origin 拒绝；重绑定域名不能获得回环自动 Gate、读取文件或执行 Cookie 写操作 |

## 9. 自动化检查与报告留存

在冻结的审核分支运行。工具未安装或需要网络时，应在受控 CI/审核机安装固定版本，并在报告中记录工具版本和公告数据库时间。

```powershell
# 基线与补丁卫生
git rev-parse HEAD
git status --short
git diff --check

# Rust 质量和测试
cargo fmt --all -- --check
cargo clippy --locked --all-targets -- -D warnings
cargo test --locked
cargo build --locked --release

# Rust 供应链；在能更新公告数据库的受控环境运行
cargo deny check
cargo audit

# 前端采用锁文件的全量检查
Set-Location frontend
npm ci
npm run check
npm audit --audit-level=moderate
```

建议的补充流水线：

- 对整个 Git 历史运行支持脱敏输出的秘密扫描器；命中项需要验证历史是否公开，不能只删除当前文件。
- 为 Release 生成 CycloneDX 或 SPDX SBOM，并与二进制/镜像 Digest 一起发布。
- 在 Linux 与 Windows 分别运行路径、权限和事务测试；S3 集成测试使用一次性 Bucket/Prefix。
- 将 Rust fmt、Clippy、测试、Deny/Audit 增加到受保护分支 CI。当前仓库可见前端工作流，正式审核需确认后端检查不是只靠本地执行。

报告必须保存命令、退出码、工具版本、运行时间和完整脱敏输出。在线公告数据库不可用时可以保留离线结果，但不能据此声称“当前没有已知漏洞”；应注明数据库快照时间并补跑在线检查。

## 10. 静态预审候选项与优先验证假设

本节来自编制文档时对**未冻结、未提交工作树**的静态走查，作用是安排正式审核顺序。表中的级别是初始建议，不是最终漏洞定级；除非写明“本机已观察”，均需在冻结基线上通过动态用例证明可利用性和影响。

### 10.1 静态预审候选项

| ID | 初始级别 | 静态观察与代码证据 | 正式审核目标/修复方向 |
| --- | --- | --- | --- |
| PRE-01 | 高候选 | `/api/login`、`/api/gate` 未挂载 `auth.rs` 的固定窗口 `RateLimiter`；持久限制执行“检查封禁 → 异步 Argon2 → 记录失败”，见 `src/app.rs`、`src/auth.rs`、`src/login_security.rs` | 以并发屏障验证同一 IP 能否在阈值落盘前同时进入昂贵验证；将检查、计数与发放资格做原子状态转换，并设置入口级有界速率/队列 |
| PRE-02 | 中高候选 | Account/WebDAV 失败状态按“入口类型 + IP”聚合，同一 IP 上任一该类账号成功可能清零其他账号攻击产生的计数，见 `src/login_security.rs::record_success` | 用两个账号/挂载交替成功与失败复现；状态至少纳入目标主体标识，同时处理 NAT 误伤、隐私和记录容量 |
| PRE-03 | 高候选 | 管理员用户名、管理员密码和全局网页密码更新依赖已有管理员 Session，未见当前密码/2FA 重新认证；敏感配置变更没有完整安全事件，见 `src/admin_api.rs::update_admin_account` | 验证被窃长期 Session 的影响；高风险变更要求近期重新认证，并记录不含秘密的结构化管理审计事件 |
| PRE-04 | 高候选 | 恢复码先通过异步 Argon2 验证，随后在独立配置更新中删除，见 `src/auth.rs::login_handler` | 两个并发请求使用同一恢复码，必须至多一个成功；采用原子消费或恢复码版本/锁 |
| PRE-05 | 高候选 | 登录/网页 Gate/文件夹解锁先复制旧哈希或 2FA 状态，再异步验证并签发；改密、启用 2FA 或改锁后清理旧令牌，可能与仍在执行的旧验证交错，见 `src/auth.rs`、`src/admin_api.rs` | 用并发屏障验证旧凭据请求能否在清理后签发新令牌；为凭据/授权增加 epoch 并在签发和每次使用时校验，或将验证与签发绑定同一版本事务 |
| PRE-06 | 高候选 | 文件夹锁匹配请求位于锁路径之下，但对锁目录的祖先执行删除/重命名/移动/复制时，单项和批量处理未见与后代锁双向重叠检查，见 `src/config.rs`、`src/api.rs`、`src/batch_operations.rs` | 在未解锁状态操作含锁子目录的父目录；树形变更必须拒绝，或在全部相关锁已授权后事务性迁移锁配置 |
| PRE-07 | 高候选 | 运行时锁路径比较区分大小写；配置校验的部分 Windows 重叠比较却忽略大小写。配置路径校验还接受 `.`，而运行时规范化会折叠它，见 `src/config.rs`、`src/config/validation.rs`、锁/WebDAV 管理 API | 在 NTFS、大小写敏感卷和实际网络盘测试大小写、尾随点/空格、`.` 与 Unicode 别名；所有配置先经唯一后端感知的规范化，再持久化和比较 |
| PRE-08 | 条件高候选 | 本地解析会拒绝最后存在祖先是链接并检查 canonical 根边界，但未使用逐组件 no-follow 句柄；指向同一存储根内其他目录的中间链接和检查/打开竞态需验证，见 `src/storage/path.rs`、`src/storage/response.rs` | 若挂载可被外部进程修改，测试跨 Share/锁别名及 TOCTOU；优先使用 handle-relative/no-follow 解析，或把“挂载由 Ycloud 独占且不得含链接”设为可验证部署前提 |
| PRE-09 | 中高候选 | 归档票据保存存储和文件集合，不保存发起主体；消费时重新查文件夹锁，但未见重新执行 StorageAction::Download，见 `src/archive.rs` | 交换票据、注销、撤权和禁用存储后消费；票据绑定主体/Session/授权 epoch，消费时复查权限并确保 URL/Trace 日志脱敏 |
| PRE-10 | 中候选 | 上传批次票据绑定 storage/path/size，但不绑定主体；每次上传仍会查 Upload 权限，见 `src/upload_batch.rs` | 同存储两个有上传权主体交换、抢占和取消票据；绑定主体/Session，并为票据查询参数和访问日志脱敏 |
| PRE-11 | 高候选（平台/部署） | Windows 的 `secure_file_permissions` 分支为空，初始明文凭据、配置、备份和安全日志依赖目录继承 DACL，见 `src/config/persistence.rs`、`src/login_security.rs`。本机预审观察到当前开发目录对普通已认证用户存在继承读或修改权限，该证据不能泛化到其他部署 | 在正式 Windows 服务目录用第二个低权限账号实测；显式设置仅服务身份、Administrators、SYSTEM 所需 ACL，并验证所有主/备份/临时文件 |
| PRE-12 | 高候选 | 普通 S3 凭据更新只保存一次配置，`.bak` 可能保留更新前的可解密密文；待添加/删除路径另有双写清理，见 `src/state.rs` | 轮换前后检查主/备份；明确安全备份历史策略，或刷新旧备份，并要求 Provider 端立即吊销旧凭据 |
| PRE-13 | 高候选 | 旧明文配置迁移通过连续保存两次擦除明文备份；两次保存之间崩溃可能留下“加密主配置 + 明文备份”，见 `src/config/persistence.rs` | 在两个发布点间故障注入并重启；启动时主动检测所有恢复副本的明文敏感字段，迁移/擦除必须可恢复且原子 |
| PRE-14 | 中高候选 | 自建 S3 白名单可以显式批准 HTTP Origin；精确 Origin 比较不固定 DNS 解析结果，见 `src/config/runtime.rs`、`src/s3_backend/client.rs` | 使用假凭据验证明文、DNS 变化、重定向、代理、TLS/SNI；考虑独立 `ALLOW_INSECURE_S3_HTTP` 与持续 UI 警告，生产结合出站防火墙 |
| PRE-15 | 中候选 | 登录日志覆盖认证事件，但未覆盖 S3/权限/密钥等管理员配置变更；默认 Trace 在 Debug 下可能包含带票据、游标或文件名的完整 URI，见 `src/login_security.rs`、`src/app.rs` | 实测 `RUST_LOG=debug`；Trace 只记录路由模板、方法、状态和请求 ID；增加脱敏管理员操作审计 |
| PRE-16 | 中候选 | 本地目录分页限制保留内存，但每页仍可能扫描完整目录；单文件 HTTP 上传也缺少明确的请求级全局/单主体 admission gate | 大目录和并发慢上传测试 CPU、I/O、FD、临时文件及排队；增加列表/上传专用有界并发、超时和 429 |
| PRE-17 | 中候选 | `IO_CONCURRENCY`、`MAX_LIST_ENTRIES`、常规/上传超时和磁盘保留等运行环境值主要做类型解析，合理非零上下界需复核，见 `src/config/runtime.rs` | 对极端值做启动矩阵；给可配置项宽松但有限的部署 envelope，避免关闭安全超时或生成巨型响应 |
| PRE-18 | 中候选 | `S3StorageConfig` 有脱敏 Debug，但包含已解密 TOTP/密码哈希等内容的上层配置类型仍可被未来 Debug 输出；进程内 S3 Secret 是可克隆 `String` | 全局搜索 Debug/Trace，增加脱敏包装/Debug；限制 Core Dump、调试和崩溃报告，明确静态加密不保护进程内存 |
| PRE-19 | 中候选 | 后端依赖检查、Rust 测试和 Clippy 未见进入当前 GitHub Actions；Cargo Dependabot、SBOM、产物签名和来源证明也未见完整配置 | 把 Rust 门禁纳入受保护分支；固定工具和 Action；生成 SBOM/校验和；持续复核 RustSec 例外 |
| PRE-20 | 中候选 | TOTP 校验接受当前计数器前后各一个窗口，但未记录已消费计数器，同一验证码可在窗口内重复创建 Session，见 `src/totp.rs`、`src/auth.rs` | 并发和顺序重放必须至多一次成功；保存短期已消费 counter，集群中使用共享原子状态 |
| PRE-21 | 中候选 | 初始凭据文件同时包含管理员与网页门禁初始密码，只要其中任一密码变化就会删除该文件；另一默认凭据可能仍有效但提醒消失，见 `src/admin_api.rs`、`src/config/persistence.rs` | 分别只轮换一种凭据验证告警；独立持久化两种“已轮换”状态，明文文件删除不能代替安全状态管理 |
| PRE-22 | 中低候选 | WebDAV 未知、禁用和已知挂载返回路径不同；用户名不匹配可在 Argon2 前返回，见 `src/webdav.rs` | 测量状态码和时间分布；未知用户名执行同成本虚拟验证，并按产品策略统一外部错误，内部保留真实原因 |
| PRE-23 | 条件高候选 | `public_proxy_config` 在回环 Bind 时直接返回本地模式，可能忽略同机反向代理使用的 `PUBLIC_BASE_URL`/可信代理设置，见 `src/config/runtime.rs` | 明确部署模式而非仅从 Bind 推断；测试“回环应用 + 同机公网代理”，确保真实 IP、Host/Proto、HSTS 和 Gate 策略不退化 |
| PRE-24 | 低到中候选 | TOTP Secret、恢复码、`/api/me` 和管理员信息未见统一 `Cache-Control: no-store, private` | 审核代理/浏览器缓存；对认证、身份、管理员和秘密响应统一 no-store，代理明确不缓存 `/api/*` |
| PRE-25 | 高候选 | 本地挂载目录先做词法去重/嵌套判断，后端稍后才 canonicalize；多个 S3 实例也可能配置相同 Origin/Bucket 与重叠 Prefix，见 `src/storage_catalog.rs`、`src/storage.rs`、`src/config/validation.rs` | 枚举全部启用实例按物理身份/规范对象命名空间比较；不同授权域不得指向同一或嵌套数据，或必须合并授权并持续告警 |
| PRE-26 | 高候选 | 非 public 模式按 peer IP 放行而未见 Host 白名单；本地 CSRF Origin 由请求 Host 推导，回环无网页密码时还有自动 Gate，见 `src/security.rs`、`src/auth.rs` | 在回环/LAN 动态验证恶意 Host 与 DNS Rebinding；引入明确允许的 Host/Origin，自动 Gate 不能由重绑定域名继承 |

### 10.2 仍需验证的总体边界

1. **同源预览隔离。** 本地/S3 响应会把 SVG 等类型降级，但仍需动态验证 PDF、文本、媒体和伪造扩展名不会在 Ycloud 同源上下文获得脚本能力。
2. **权限变更和长期状态。** Session 为内存态且有绝对 TTL；登录封禁为持久态。需验证账号/权限/存储变化后的即时撤销，以及多实例拓扑是否被支持或明确拒绝。
3. **局域网明文边界。** `ALLOW_LAN_HTTP=true` 是显式选择，但 WebDAV Basic、Session 和上传内容都会以明文穿越局域网。审核应把它标为受信网络模式，不得作为公网或不可信 Wi-Fi 部署方案。
4. **主密钥恢复和轮换。** AES-GCM、DPAPI/Unix 权限解决静态配置泄露的一部分风险，不防运行账号失陷。必须验证密钥与配置成对备份、服务账号迁移和轮换流程。
5. **S3 保留空间的独占性。** S3 事务日志有严格格式和 ETag 校验但没有独立 MAC；应确认配置 Prefix 与 `.ycloud-system` 只由 Ycloud 身份写入，或为恢复日志增加认证保护。
6. **依赖公告例外。** `deny.toml` 对 `RUSTSEC-2026-0253` 有可达性说明；审核必须用当前 AWS SDK 依赖树重新确认 S3 Express 路径确实关闭，并跟踪上游修复，不能只沿用旧报告。

## 11. 部署审核基线

### 11.1 公开 HTTPS 模式

- [ ] Ycloud 不直接暴露在公网，只允许来自反向代理的连接。
- [ ] `PUBLIC_BASE_URL` 为唯一、精确的 HTTPS Origin，不含路径和查询。
- [ ] `TRUSTED_PROXY_IPS` 只包含实际代理的固定地址，不使用任意网段或客户端可控值。
- [ ] `SECURE_COOKIES=true`，代理强制 HTTP 跳转 HTTPS，证书链和自动续期正常。
- [ ] 代理覆盖请求体大小、连接/读写超时、慢请求限制和访问日志脱敏。
- [ ] 应用端口被主机防火墙或容器网络隔离，外部无法绕过代理直连。
- [ ] HSTS 策略与域名子域实际部署匹配。

### 11.2 局域网明文模式

- [ ] 仅在可信、隔离的 LAN 使用，并记录接受明文 Session/WebDAV 密码的风险。
- [ ] 不通过端口映射、UPnP、公网 IPv6 或错误防火墙规则暴露。
- [ ] 不在访客 Wi-Fi、共享办公网或存在恶意终端的网络使用。
- [ ] 明确允许的 Host/Origin；从浏览器验证攻击者域名和 DNS Rebinding 不能借 LAN/回环地址获得访问。
- [ ] 若风险不可接受，改为内网 HTTPS 反向代理或 VPN。

### 11.3 主机、容器和文件权限

- [ ] 使用独立非管理员服务账号运行 Ycloud；存储和配置只授予必要 ACL。
- [ ] 配置、主密钥、安全日志、事务区和备份不被 Web Server 作为静态文件发布。
- [ ] 容器采用非 root、最小 Capability、受控可写卷和只读 Secret 文件；镜像 Digest 固定。
- [ ] `YCLOUD_CONFIG_KEY_FILE` 由 Secret 管理器只读挂载；非必要不使用环境变量明文密钥。
- [ ] 禁止不受控 Core Dump；日志、Swap、备份介质和运维采集均按敏感数据保护。
- [ ] 仅允许 Ycloud 出站访问批准的 S3/DNS/时间源；管理面不向互联网任意开放。
- [ ] NTP/时间同步有监控，避免 TOTP 和签名因时钟漂移异常。

### 11.4 备份与恢复

- [ ] 用户文件、`config.json`、安全状态、容量/事务状态和主密钥按一致性要求备份。
- [ ] 备份加密并独立控制访问；恢复密钥不与唯一备份放在同一故障域。
- [ ] 至少在全新主机完成一次恢复演练：配置可解密、管理员可 2FA 登录、S3 可连接、权限不变化。
- [ ] 记录 RPO/RTO、密钥丢失处置、凭据轮换和异常 Multipart/事务清理步骤。

## 12. 发现项记录模板

```markdown
### YCLOUD-SEC-YYYY-NNN：简短标题

- 严重度：Critical / High / Medium / Low / Info
- 状态：待审核 / 已确认 / 已修复待复测 / 已关闭 / 接受风险
- 置信度：高 / 中 / 低
- CWE / OWASP：如适用
- 影响版本与提交：
- 影响组件/路由：
- 所需攻击者权限：
- 前置条件：
- 观察结果：
- 安全影响：
- 代码根因：
- 脱敏证据与请求 ID：
- 修复建议：
- 回归测试：
- 负责人 / 截止日期：
- 风险接受人 / 到期日：如适用
```

发现项必须描述实际安全边界变化，不能把代码风格、产品偏好或第三方平台自身责任包装成 Ycloud 漏洞。相反，任何跨权限、跨存储、根外路径、秘密泄露或默认公网暴露都不能降级为“仅加固”。

## 13. 执行记录

### 13.1 测试记录

| 日期 | 基线 | 用例/命令 | 环境 | 结果 | 证据位置 | 执行人 |
| --- | --- | --- | --- | --- | --- | --- |
| 待填写 |  |  |  |  |  |  |

### 13.2 发现项总表

| ID | 标题 | 等级 | 状态 | 影响范围 | 负责人 | 截止/到期 | 复测证据 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 待填写 |  |  |  |  |  |  |  |

### 13.3 风险接受

| ID | 接受理由 | 补偿控制 | 剩余风险 | 接受人 | 到期日 | 复审触发条件 |
| --- | --- | --- | --- | --- | --- | --- |
| 待填写 |  |  |  |  |  |  |

## 14. 修复时限和发布门槛

建议时限可按项目发布节奏收紧，但不得无负责人延期：

| 等级 | 首次响应 | 建议修复/缓解时限 |
| --- | --- | --- |
| Critical | 立即 | 24 小时内缓解并停止受影响发布；完整修复后独立复测 |
| High | 1 个工作日 | 7 天内修复；公网 Release 前必须关闭 |
| Medium | 3 个工作日 | 30 天内修复或书面接受 |
| Low | 下个维护周期 | 90 天内处理或记录不处理理由 |

Release 通过必须同时满足：

- [ ] 冻结基线和实际发布产物一致。
- [ ] 全部 `P0` 与 `P1` 审核项有证据；无法执行的项目有原因和补偿验证。
- [ ] 未关闭的 Critical/High 为 0。
- [ ] Medium 均有负责人、期限；接受风险有到期日和复审条件。
- [ ] Rust 与前端完整检查通过；依赖公告数据库为近期快照。
- [ ] 本地、S3、WebDAV、2FA、权限矩阵、票据和路径边界的动态用例通过。
- [ ] 公网或局域网部署模式与实际网络边界一致，没有应用端口旁路。
- [ ] 主密钥、配置和用户数据的恢复演练通过。
- [ ] 生产日志与错误响应抽检无秘密和内部路径泄露。
- [ ] 修复项由非原实现者复测，且回归测试进入 CI。

## 15. 审核交付物

最终审核包至少包含：

1. 本文件的已填写副本。
2. 冻结提交、Tag、产物哈希和脱敏部署清单。
3. 路由/权限矩阵、数据流和信任边界最终版。
4. 自动化检查原始脱敏报告与工具版本。
5. 动态测试记录、请求 ID、截图和复现环境说明。
6. 发现项、修复提交、回归测试和独立复测证据。
7. 依赖/SBOM、公告例外及到期复审记录。
8. 主密钥与配置恢复演练记录。
9. 最终风险接受与发布签字。

## 16. 建议执行顺序

1. 冻结提交和隔离测试环境。
2. 先审认证、Session、权限矩阵和管理 API。
3. 再审本地路径、上传/归档票据、预览和文件夹锁。
4. 接着审 S3 出站边界、秘密层、WebDAV 和代理部署。
5. 并行完成依赖、CI、日志、资源耗尽和恢复测试。
6. 修复 Critical/High，补回归测试，由另一人复测。
7. 完成主密钥恢复演练和实际部署核验后再作 Release 结论。

该顺序优先验证一旦失败就可能导致越权、根外访问或秘密泄露的安全边界，再处理纵深防御和运维完整性。
