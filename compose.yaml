name: ycloud

services:
  ycloud:
    build: .
    image: ycloud:local
    restart: unless-stopped
    user: "10001:10001"
    ports:
      - "18473:18473"
    volumes:
      - ./data:/var/lib/ycloud
