# Ycloud 身份与访问

Ycloud 将后台管理身份、文件访问身份、登录保护策略和认证记录明确分开，避免把账号、权限与日志混为同一概念。

## Language

**管理员**：
唯一能够进入管理后台并配置系统、存储和用户权限的身份。
_Avoid_: 管理账号、超级用户

**用户账号**：
由管理员创建，只能按授权访问文件与存储源、不能进入管理后台的身份。
_Avoid_: 普通账号、子账号

**登录保护**：
连续认证失败后，按登录入口和 IP 自动限制后续尝试的安全策略。
_Avoid_: 登录安全、访问限制

**访问日志**：
管理后台、用户账号、文件首页和 WebDAV 的认证结果与人工 IP 限制记录；不表示文件读写操作审计。
_Avoid_: 登录安全、安全日志、文件审计日志

**存储源**：
管理员添加到 Ycloud、由不可变 `storage_id` 唯一标识的独立文件命名空间。
_Avoid_: 默认存储、存储后端

**存储源状态**：
存储源在后台呈现的启用、停用或异常状态；访客访问不是状态，而是独立访问策略。
_Avoid_: 在线开关、访客状态

**访客访问**：
决定通过文件首页验证的访客能否进入某个已启用存储源的配置；不影响管理员和用户账号的授权。
_Avoid_: 默认存储、公开存储
