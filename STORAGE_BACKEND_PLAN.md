# 远程存储后端规划

本文记录远程对象存储的目标、已完成边界与后续实施顺序。

## 独立的两层连接

Ycloud 的用户入口和存储后端互相独立，不要求使用相同的域名、协议或网络：

- 用户入口支持通过反向代理后的 HTTPS 域名访问 Ycloud，也保留受控的 IP、端口和局域网部署方式。
- 存储后端支持通过 EasyTier 私有地址连接家庭 RustFS，也支持通过 HTTPS 域名连接 RustFS 或其他兼容的 S3 存储桶。

支持的组合包括：

| Ycloud 用户入口 | S3/RustFS 后端入口 |
| --- | --- |
| HTTPS 域名 | EasyTier 私有 IP |
| HTTPS 域名 | HTTPS 存储桶域名 |
| 受控 IP 与端口 | EasyTier 私有 IP |
| 受控 IP 与端口 | HTTPS 存储桶域名 |

## S3 后端配置

- 后端类型：本地文件系统或 S3 兼容对象存储。
- 第一批服务商预设：阿里云 OSS、腾讯云 COS、MinIO/RustFS、S3 通用兼容。预设不是四套协议实现，而是同一 S3 驱动上的地址、区域和寻址规则。
- Endpoint：接受 EasyTier 私有地址或完整 HTTPS 域名。
- Bucket、Region 和路径前缀。
- 默认使用 path-style addressing，避免强制依赖通配符 DNS 和通配符证书。
- 使用独立的最小权限服务账户；密钥不得写入仓库或输出到日志。
- 公网域名必须验证 HTTPS 证书，不能提供忽略证书错误的普通管理选项。
- 私有明文 Endpoint 仅允许显式启用，并限制为 EasyTier 或其他可信私有网段。

## 数据路径与安全边界

- 上传、下载、Range、预览和打包下载必须流式经过 Ycloud，不在 VPS 缓存完整对象。
- 默认不向浏览器签发直连存储桶的预签名 URL，避免绕过 Ycloud 的权限、日志和速率限制。
- EasyTier 用于避免直接暴露家庭 RustFS，并解决无备案域名或家庭网络不可直接入站的问题；它不是 Ycloud 域名访问能力的替代品。
- RustFS/S3 不复用本地文件系统的原子重命名事务假设。对象覆盖、复制、移动和删除需要独立的 S3 事务与失败恢复设计。
- 家庭存储离线时，Ycloud 应明确显示存储后端不可用，不把网络故障误报为文件不存在。

## 已确认的多存储架构调整（分阶段实施中）

Ycloud 继续作为主架构，负责账户、授权、文件夹锁、WebDAV、日志、限速、文件界面和操作编排。在 Ycloud 内新增统一存储管理层，由它管理存储实例及公共规则；本地文件系统和各类 S3 存储作为独立适配器接入，不让服务商差异进入主业务代码。

职责拆分固定为：

- Ycloud 主业务层：认证授权、访问策略、管理后台、文件操作流程和用户界面。
- 统一存储管理层：存储注册、配置校验、连接测试、启停状态、容量配额、健康状态、统一错误和后端路由。
- 本地存储适配器：本地路径边界、符号链接与重解析点防护、磁盘安全余量、原子提交和本地事务恢复。
- S3 存储适配器：签名、Endpoint 与寻址规则、分页、ETag、条件请求、对象事务和服务商兼容差异。阿里云 OSS、腾讯云 COS、MinIO/RustFS 与通用 S3 共享同一 S3 引擎，只使用独立服务商配置预设，不复制四套实现。

文件浏览采用独立“存储空间”，不把本地与多个 S3 后端合并成一个虚拟根目录：

- 每个存储实例拥有不可变的 `storage_id`、可修改显示名称、后端类型、独立根路径或 Prefix、容量限制和运行状态。
- 文件浏览器右上角提供存储切换器；切换器本身就是存储边界，不再把存储名称放进路径栏。
- 路径展示保持现有形式：根目录为 `/`，子目录为 `/游戏/`、`/游戏/Switch/`。后端始终使用 `storage_id + path` 定位文件，不能只依赖前端当前选择。
- 切换存储后回到该存储的 `/`，并清空当前选择、搜索结果、目录缓存和文件夹解锁状态，防止跨存储状态串用。
- 文件夹锁、WebDAV 挂载、容量、操作日志和访问记录必须绑定 `storage_id`；一个 WebDAV 挂载只能指向一个存储实例及其内部路径。
- 单个存储离线或配置错误不能拖垮其他存储。文件浏览器应显示该存储不可用，而不是显示空目录或回退到本地存储。
- 第一阶段禁止跨存储复制和移动，避免隐式产生大流量、半成品与删除风险。后续如增加跨存储传输，必须作为显式流式任务；跨存储移动按照“复制、校验、再删除源对象”执行。
- 搜索、打包下载、批量操作和普通文件操作默认限制在当前存储空间内，不构建混合命名空间。

### 存储启停、访客访问与轻量账号授权（已实施）

多存储不引入完整多用户、用户组、角色继承或文件级 ACL 系统，只增加由管理员管理的普通账号与按存储授权：

- 管理员是唯一可以进入管理后台、管理存储配置、创建用户账号和分配权限的身份。
- 每个存储源独立配置是否启动以及是否允许访客访问。后台把运行结果统一显示为“启用、停用、异常”，访客访问不是运行状态。
- 文件页顶栏列出已添加、已启用且运行可用的存储源；不展示后台状态。访客可以直接进入允许访客访问的存储源，受限存储在选择时要求管理员或已授权用户账号登录。
- 普通账号只能进入文件浏览界面，不能访问任何管理后台页面或管理 API。
- 管理员按照 `账号 + storage_id` 分配浏览、下载、上传、新建目录、重命名、移动、复制和删除权限；权限由后端逐请求校验，前端只负责呈现可用操作。
- 存储切换器位于文件页全局顶栏；切换后回到目标根目录并清空搜索、分页和选择状态。
- 提供“普通用户”权限模板，默认仅包含管理员指定存储的浏览和下载；上传、重命名和删除由管理员明确增加。
- 系统不向用户暴露默认存储或“设为默认”操作；未指定 `storage_id` 时，后端只在当前身份实际可浏览的启用存储中选择首个可用项。
- 账号状态、密码处理、会话失效和登录限流属于系统安全机制，不作为可赋予权限。账号删除、密码修改或权限收回后，旧会话必须自动失效；登录错误限制使用统一的服务端策略。
- WebDAV 凭据继续独立于网页普通账号，并绑定单一 `storage_id + path`，不继承网页账号的存储切换能力。

实施顺序中的多存储核心与轻量账号授权已经完成：`storage_id` 数据模型、统一存储注册表、本地与多个 S3 实例、API/WebDAV/文件夹锁绑定、管理后台配置、文件浏览器切换器，以及普通账号的后台创建、停用、改密和按存储授权均已连通。实施过程没有迁移或删除现有用户数据；旧单后端配置只迁移为等价实例配置。

当前 `schema_version: 9` 最多保存 16 个独立存储实例、至多一个待添加实例，以及最多 100 个用户账号。本地实例通过唯一 `mount_id` 绑定部署声明的目录；后台使用文本框填写路径，后端将规范化后的路径解析到 `STORAGE_PATH`/`LOCAL_STORAGE_MOUNTS` 已暴露给进程的目录，而不是依赖前端提交内部 `mount_id`。一个挂载地址只能创建一个存储源。每个实例保存独立启停与访客访问策略；用户账号只能提出已获浏览权限的存储身份，管理员可以访问全部已启用存储。批量操作、归档票据、文件夹锁、用户账号权限和 WebDAV 挂载均绑定 `storage_id`。切换存储会回到目标根目录并清空页面选择、分页与搜索状态，路径仍保持 `/目录/`，不会混入存储名称。删除配置不会删除后端文件；存在 WebDAV、文件夹锁或用户权限引用时必须先解除引用。

## 强制安全验收

远程 Endpoint 属于服务器主动访问的高风险信任边界，实施时必须同时满足以下要求：

- 防止 SSRF：拒绝云元数据地址、环回、链路本地、组播、未授权私网以及解析后越界的地址。
- 防止 DNS 重绑定：连接前后都校验解析结果；重试和重新解析不能绕过原有地址策略。
- 禁止跨主机重定向，避免把 S3 签名、凭据或请求内容带到另一个主机。
- MinIO/RustFS 与通用 S3 Endpoint 必须精确匹配部署环境变量 `S3_ALLOWED_ENDPOINTS`；后台只能收窄或选择该边界，不能自行扩展。阿里云和腾讯云预设仅接受各自官方 HTTPS Endpoint。
- 公网 Endpoint 强制 HTTPS 和正常证书验证；不提供“一键忽略证书错误”。
- EasyTier、同机或其他私网 HTTP 必须由管理员显式启用并配置精确允许的 CIDR 或主机，而不是放开全部私网。
- Access Key、Secret Key 和签名信息必须在日志、错误详情和管理 API 响应中脱敏；配置文件和容器 Secret 使用最小文件权限。
- 持久化配置使用 `schema_version: 9`，保存有界的独立存储实例、各实例启停与访客访问策略、至多一个待添加实例、本地部署 `mount_id`，以及用户账号的按存储权限；旧唯一后端安全迁移为等价 `primary` 实例，不移动用户文件。
- 远程存储创建时先在保留前缀完成列举、写入、读取、复制和删除验证，再原子保存为待添加配置；测试请求具有严格的连接、读取和总超时。
- S3 请求设置固定并发、重试、请求体和响应体边界，防止故障后无界重试或占满 1 核 1 GiB 级服务器。

## 当前实施状态

- 已完成强类型 v9 多存储与用户账号配置、部署本地挂载白名单、独立启停和访客访问策略、服务商预设校验、Endpoint 部署白名单、受限 S3 客户端及完整添加探测。
- 已完成目录列举、文件/目录元数据判定、带 Range 的条件流式下载，以及单文件流式暂存、校验、备份和服务端复制提交。
- 已完成显式目录创建，以及文件和目录的不覆盖复制、移动与条件删除。递归目录事务使用版本化清单，一次最多 1000 个对象、清单最大 4 MiB、每 16 个对象保存一次进度；移动在所有目标复制并验证后才删除源，删除先复制到内部暂存前缀再移除用户对象。源删除失败时保留两份，优先避免数据丢失。S3 前缀变更没有伪装成原子操作，目标对象会逐项出现。
- 上传替换会在 `.ycloud-system/transactions` 写入有版本、大小上限和条件 ETag 更新的事务日志。恢复最多处理 1000 条记录，只接受可由对象大小与 ETag 证明的旧版本或新版本状态；第三种状态会保留临时对象和备份并拒绝继续写入。
- 已建立本地与 S3 共用的路径级边界，网页目录列表、下载、预览、健康检查、新建、上传、普通文件复制/移动/删除、批量入口、WebDAV 受支持方法和流式 ZIP 均通过该边界。S3 上传要求可验证的 `Content-Length`，不会在 VPS 缓存未知长度的完整对象。ZIP 下载会重新校验准备阶段记录的大小、修改时间和 S3 ETag，并逐块写入单个打包任务。
- 管理后台提供本地、阿里云 OSS、腾讯云 COS、MinIO/RustFS 和通用 S3 五种入口。单纯连接测试不保存密钥；保存待添加 S3 前会在 `.ycloud-system/activation-tests` 完成条件写入、校验、复制和删除。管理 API 仅返回非敏感连接参数及密钥存在标记。
- 已完成两阶段安全添加：保存待添加配置不改变已有实例；确认添加时重新验证并恢复该后端事务，先持久化实例再发布到运行时注册表。添加不迁移或删除数据，失败不影响已有实例。启动时单个 S3 初始化失败会被隔离为不可用状态，不会回退到另一命名空间，也不会阻止其他可用实例启动。
- 已完成每个本地与 S3 实例独立的逻辑容量上限。启动或添加时扫描一次用户数据，上传和复制按净增长预留，删除释放、移动保持不变；复制提交前重新核对源大小。容量上限不替代本地磁盘固定安全余量，也不替代对象存储服务端硬配额。S3 只统计 Ycloud 独占 Prefix 下的用户对象并排除内部事务对象，外部写入者会使进程内计数失真，因此生产环境必须使用独占 Prefix，并建议同时设置 RustFS、MinIO 或云厂商硬配额。
- AWS SDK 1.143.0 仍间接固定 `lru 0.16.4`，命中 `RUSTSEC-2026-0253`。该依赖只位于 SDK 的 S3 Express 身份缓存；Ycloud 为所有 S3 客户端关闭 S3 Express 会话认证，因此受影响路径不可达。`cargo-deny` 仅对这一编号保留有理由的临时例外，全局 `unsound = "all"` 不变；AWS SDK 接受 `lru >= 0.18.2` 后必须立即删除例外。
- 目录事务当前使用单次 `CopyObject`，因此在创建事务前拒绝包含超过 5 GB 对象的目录；超过该边界需要后续实现分段复制。RustFS `1.0.0-beta.12` 与腾讯云 COS 已通过独立测试桶的真实兼容性冒烟测试；AWS、阿里 OSS 和 MinIO 仍需分别完成真实服务验证，所有后端仍需继续补充故障注入测试。
- Bucket、Prefix 和 Object Key 逐层规范化，禁止越过配置前缀或访问 Ycloud 内部保留命名空间。

## S3 兼容性验收

仓库提供默认忽略的真实 S3 冒烟测试 `s3_backend::tests::s3_compatibility_smoke`。测试要求一个与正式数据隔离的现有 Bucket，并在每次执行时创建随机独占 Prefix；它依次验证列举、条件写入、HEAD、下载、覆盖、文件复制/移动/删除、目录复制/移动/删除、事务恢复与容量扫描。不要使用正式 Bucket、正式 Prefix 或根凭据，测试身份应只获得测试 Bucket 所需的最小 S3 权限。

PowerShell 中只为当前会话设置凭据，然后单线程显式运行被忽略的测试：

```powershell
$env:YCLOUD_S3_SMOKE_ENDPOINT = "http://127.0.0.1:9000"
$env:YCLOUD_S3_SMOKE_BUCKET = "ycloud-test"
$env:YCLOUD_S3_SMOKE_REGION = "us-east-1"
$env:YCLOUD_S3_SMOKE_PROVIDER = "minio"
$env:YCLOUD_S3_SMOKE_ADDRESSING_STYLE = "path"
$env:YCLOUD_S3_SMOKE_PREFIX = "compatibility/"
$env:YCLOUD_S3_SMOKE_ACCESS_KEY_ID = "<test-access-key>"
$env:YCLOUD_S3_SMOKE_SECRET_ACCESS_KEY = "<test-secret-key>"
cargo test --locked s3_backend::tests::s3_compatibility_smoke -- --ignored --nocapture --test-threads=1
```

`YCLOUD_S3_SMOKE_PROVIDER` 支持 `alibaba_oss`、`tencent_cos`、`minio` 与 `s3_compatible`。`YCLOUD_S3_SMOKE_ADDRESSING_STYLE` 支持 `path` 与 `virtual_hosted`；未设置时，阿里云 OSS 和腾讯云 COS 默认使用虚拟主机寻址，其余类型默认使用路径寻址。正式服务商测试必须使用 HTTPS 官方区域 Endpoint，并由对应 Provider 的生产配置校验约束地址与寻址方式。

RustFS 使用 S3 API 端口（默认 `9000`），不是管理控制台端口（默认 `9001`），寻址方式使用 Path Style。正常 `cargo test` 不读取这些变量，也不会连接外部对象存储。当前冒烟测试已在 RustFS `1.0.0-beta.12` 和腾讯云 COS 上验证列举、受控分页、条件写入、HEAD、下载、覆盖、文件与目录的复制/移动/删除、事务恢复和容量扫描。目录与事务列举按服务端继续令牌分页，只有实际对象或事务数越过 Ycloud 固定上限时才拒绝，不能把服务端的单页截断标记直接解释为越界。
- Endpoint、凭据或 Bucket 变化后终止旧连接池和未授权会话，不让旧凭据继续长期生效。
- 加入针对 SSRF、DNS 重绑定、恶意重定向、证书错误、凭据泄露、路径越界和连接耗尽的自动化测试后，远程存储功能才可验收。
