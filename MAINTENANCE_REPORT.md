# Ycloud 维护报告

审查权重：安全 40%、性能 30%、稳定 30%。结论基于当前代码、测试和依赖锁文件，不记录历史流水账。

## 本轮已完成

### 增加

- 目录使用绑定存储、路径、搜索和排序条件的不透明游标；限制搜索为 256 个字符、游标为 4 KiB。
- 本地与 S3 共用有界分页收集器，内存只保留当前页候选，不再先构造完整目录列表。
- 存储容量使用持久账本；失败时标记不可信并后台核对，配置容量上限时禁止在未知容量下继续写入。
- 配置和初始凭据发布后同步父目录（Unix），降低断电后目录项丢失概率。
- 管理员会话、普通账号会话和网页/文件夹令牌增加全局及单主体数量上限。
- 分离存活探针 `/api/health` 与存储就绪探针 `/api/ready`。

### 删除

- 删除浏览 API 的伪 `truncated` 状态及前端提示；浏览已由真实分页取代。
- 删除不可变存储对象外层的异步读写锁、未使用的运行时容量改写接口和对应死测试。
- 删除 `/v2`、`login.html`、`browser.html`、`admin.html`、`preview.html` 路径兼容层；开发与生产共用正式路由。
- 删除配置迁移中两个只改版本号的空步骤，保留确实转换字段的旧配置迁移。
- 删除过期交接文档，当前结构只由架构文档和本报告描述。

### 重构

- 从 `api.rs`、`storage_backend.rs` 提取 `directory_listing.rs`，统一本地和 S3 的排序、搜索和页边界。
- 将 `s3_backend.rs` 中相互独立的客户端构造、键空间、目录分页、容量扫描、对象读取和响应体边界拆入 `s3_backend/`；统一后端入口及事务写入行为不变。
- 将 `config.rs` 拆为数据模型、业务校验、环境部署、schema 迁移、原子持久化和 Argon2 密码模块；公共导出、JSON 字段和迁移顺序保持不变。
- 容量统计从请求路径上的全量 S3 扫描改为账本、增量更新和固定周期核对。
- S3 变更失败不再让请求同步扫描整个桶；请求返回后由单一后台任务恢复准确状态。
- 配置迁移只在全部实际转换结束后写一次当前 schema 版本。

### 保留

- 保留 S3 厂商差异、WebDAV 方法兼容、旧配置实际字段迁移、本地事务区和严格资源并发上限。这些是功能或安全边界，不属于冗余。
- 保留 Rust/Axum + Vue/TypeScript；当前没有用 Go、前端状态库或数据库替换的收益依据。

## 尚需继续拆分

当前没有需要立即停服修复的已证实严重问题，但以下模块职责仍过大：

1. `s3_backend.rs`：入口已由约 83 KiB 降至约 53 KiB；上传事务、对象变更与恢复仍保持在一起，后续只有在共享状态边界清晰时再拆，避免制造跨模块耦合。
2. `storage.rs`：约 45 KiB。分离路径解析、流式响应和本地写入器，事务状态继续留在 `storage_transaction.rs`。
3. `app.rs` 与 `admin_api.rs`：分别约 40 KiB、37 KiB。先提取路由表和管理端 DTO，再按领域拆 handler；不建立无业务意义的多层 service/repository 包装。

拆分原则：一次只移动一个职责，外部 API、存储格式和权限行为保持不变；每步都必须通过全量 Rust、前端和依赖检查。

## 已知边界

- S3 目录在任意搜索和排序下仍需遍历该目录的对象页；内存已为 O(页大小)，但远端请求耗时仍为 O(目录对象数)。后续可增加短时目录索引缓存，但不能以陈旧缓存破坏一致性。
- 无状态目录游标不保存目录快照；翻页期间目录发生修改时可能看到重复或遗漏项。这是低内存方案的明确取舍。
- `aws-sdk-s3` 间接依赖 `lru 0.16.4`，命中 RUSTSEC-2026-0253。Ycloud 创建所有 S3 客户端时都关闭 S3 Express 会话认证，因此受影响缓存路径不可达；待 AWS SDK 上游更新后移除临时例外。
- AWS SDK 带来的重复 `http`、`digest` 等版本属于上游依赖树，当前不能通过删除 Ycloud 直接依赖安全消除。

## 验证状态

- Rust：全量测试 87 通过、1 个需要隔离 S3 凭据的冒烟测试忽略；严格 Clippy 通过。
- 前端：22 个测试文件、88 项测试通过；ESLint、TypeScript 和生产构建通过。
- `cargo deny check`：advisories、bans、licenses、sources 全部通过；重复依赖仅为警告。
- `cargo audit`：已在线更新并加载 1226 条 RustSec 公告，无未允许漏洞；上述 `lru` 为已记录例外。
