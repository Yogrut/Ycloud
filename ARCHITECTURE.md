# Ycloud 当前架构

本文只描述当前实现，不记录旧版本和维护过程。设计权重为安全 40%、性能 30%、稳定 30%，数据安全性与数据完整性并列优先。

登录安全状态以完整 IP 持久化到 `security-events.json`：管理员失败 3 次限制 1 小时，首页失败 5 次限制 1 小时，WebDAV 错误凭据 5 次限制 1 分钟。普通凭据错误与真正的限制状态使用不同响应；达到阈值后返回 `429` 和 `Retry-After`。管理后台可查看记录并解除单个 IP 的单个入口限制。WebDAV 挂载与文件夹锁的父、当前或子目录不得重叠，校验与配置顺序无关。

## 技术与部署边界

Ycloud 是单进程 Rust 服务，使用 Tokio、Axum 0.8 和 Tower HTTP 0.6。生产网页使用 Vue 3.5 与 TypeScript，经过 Vite 构建后写入 `static/app/`，再由 Rust 编译进可执行文件；运行时不需要 Node、数据库或外部 CDN。

`frontend/` 是唯一的网页前端源码，包含首页登录、共享主题、文件预览、文件浏览操作闭环，以及管理后台鉴权、统一布局、侧边导航、账户与访问、登录安全、传输限制、文件夹锁和 WebDAV。文件浏览支持目录导航、当前目录搜索、排序、选择、文件夹锁解锁、预览、管理员登录入口、退出、多文件顺序上传、新建目录、单文件下载、打包下载、重命名、移动、复制和永久删除。桌面端右键菜单按命中区域提供操作，并支持超过移动阈值后的方向式框选：可从文件行内容或复选框开始，向下拖动增加经过的文件行，向上拖动取消经过的已选行；按下时即阻止浏览器原生文字和图片拖拽，不绘制额外选择框，结束后只保留文件行的选中状态。外部文件拖入被忽略，不作为上传入口；移动端禁用框选并使用选中后的横向操作面板。批量接口的 `200`、`207` 和冲突结果均保留逐项明细。

管理后台通过现有管理员 Cookie 会话访问 `/api/admin/*`，未认证时在 Vue 页面内显示管理员登录门禁。账户页只保存用户明确修改的字段，真实密码不读取、不回显、不写入浏览器存储；移除已有首页密码需要二次确认，修改管理员用户名或密码后跳回文件页并要求重新登录。

登录安全数据仍以“入口 + IP”作为唯一记录，最多持久化 500 条最新状态，不伪装成不可用的无限事件流水。Vue 页面在最多 500 条的固定边界内执行时间筛选、状态分区和每页 10 条分页，不增加后端查询与数据库复杂度。管理员可以按入口手动限制 IP，限制时长复用该入口固定策略；解除操作清除失败计数和限制状态。两项操作都经过管理员 Cookie、CSRF 与 64 KiB 请求体限制，并原子写入安全状态文件。

Vue 传输限制页复用 `/api/admin/limits`，仅管理持久化的单文件上传上限、打包源文件总大小和打包条目数量。未编辑的 GiB 文本映射回服务器提供的原始字节值，避免显示精度造成配置漂移；编辑值在前端转换为安全整数，并继续接受 Rust 后端的范围和部署绝对上限复核。文件 I/O、WebDAV、Argon2、打包并发、事务锁和磁盘安全余量仍属于固定资源保护，不进入管理员可调配置。

Vue 文件夹锁页复用 `/api/admin/locks`，管理新建、编辑和删除。路径在浏览器中只做一致化展示，Rust 配置校验仍是最终边界：禁止存储根目录和无效相对路径，并拒绝与任何已启用 WebDAV 挂载的父目录、当前目录或子目录重叠。密码按 Unicode 字符计数且至少 8 位；编辑时掩码不会提交，只有明确输入的新密码才会替换哈希。锁路径或密码变化及删除都会由后端撤销对应范围的既有解锁令牌。

Vue WebDAV 页复用 `/api/admin/shares`，管理挂载的名称、存储路径、独立用户名和密码、启停状态及只读/读写权限。管理 API 只返回密码是否存在，浏览器不会收到哈希；编辑掩码不会提交，明确清空密码只允许与停用挂载同时完成。启用挂载必须具有用户名和至少 12 位密码，路径仍由 Rust 拒绝与文件夹锁的父、当前或子目录重叠。删除只移除挂载配置，不删除磁盘文件，也不改变 `/dav/{挂载名}` 下的 Basic Auth 协议。

正式网页路由为 `/`、`/browse`、`/preview` 与 `/admin/*`。旧 HTML 兼容地址进入相同 Vue 页面，迁移期的 `/v2/*` 地址永久重定向到正式地址。原生旧前端已经删除，恢复依赖 Git 历史而不是在生产包中保留两套实现。开发时 Vite 代理连接同一 Rust 服务，生产时只由 Rust 提供固定名称且不依赖外部 CDN 的静态资源。依赖使用精确版本和 lockfile，更新经静态检查、测试及构建验证后人工合并。

默认绑定 `127.0.0.1:18473`。非回环监听只支持“同机可信 HTTPS 反向代理”模型，启动时强制要求：

- `PUBLIC_BASE_URL` 是无路径和查询参数的 HTTPS 来源；
- `SECURE_COOKIES=true`；
- `TRUSTED_PROXY_IPS` 至少包含一个精确代理连接 IP。

可信局域网可显式启用 `ALLOW_LAN_HTTP=true` 并监听非回环地址。该模式只接受回环、RFC 1918、IPv4 链路本地、IPv6 链路本地及 ULA 来源，不读取代理头；它不能替代公网 HTTPS 反向代理。

公网请求只接受可信代理连接，且必须具有单一 `X-Forwarded-For`、`X-Forwarded-Proto: https` 和与公共来源一致的 Host。登录记录中的 IP 是该可信代理校验后的客户端 IP。局域网直连记录 TCP 对端 IP，并忽略客户端自行提供的代理头，防止伪造日志和绕过限流。程序不直接终止 TLS。

## 认证与权限

| 通道 | 凭据 | 权限 |
|---|---|---|
| 管理后台和网页写 API | 管理员 Cookie 会话 | 配置与全部网页写操作 |
| 网页文件入口 | 网页访问 Cookie 或管理员会话 | 列表、预览和下载 |
| 网页文件夹锁 | 目录独立解锁 Cookie | 指定目录及子目录的网页访问 |
| WebDAV | 挂载独立 Basic Auth | 挂载路径及其只读/读写边界 |

管理 API 不接受 Basic Auth。管理员账号或密码变化会撤销全部管理员会话；网页访问密码变化会撤销全部网页令牌；文件夹锁变化只撤销对应范围的解锁令牌。管理员和 WebDAV 密码至少 12 字符，网页访问密码和文件夹锁至少 8 字符，已有有效 Argon2 哈希继续使用。

所有 Cookie 使用 `HttpOnly`、`SameSite=Strict`，公网模式使用 `Secure`。修改请求的 CSRF 来源固定匹配 `PUBLIC_BASE_URL`；响应使用无内联样式例外的 CSP，并在公网模式发送 HSTS。

WebDAV 密码计算全进程并发为 1，最多 8 个 WebDAV 活动请求，密码队列等待最多 3 秒，每个客户端 IP 在 60 秒内最多 5 次认证失败。过载返回 `429` 或 `503`，不会形成无界密码队列。

## 存储路径与事务

所有网页 API 和 WebDAV 操作都通过 `StorageService` 访问磁盘。路径按组件规范化，拒绝 `..`、反斜杠、盘符、空字节、符号链接和 Windows 重解析点。服务账户必须独占存储目录，以减少本机其他用户制造 TOCTOU 的机会。

存储根目录包含保留区 `.ycloud-system`：

```text
.ycloud-system/
├── marker
├── transactions/  # 普通文件替换清单
├── uploads/       # 上传临时文件
├── copies/        # 目录复制临时树
├── backups/       # 被替换的旧普通文件
└── trash/         # 已从用户命名空间移除、等待清理的数据
```

该名称不能通过列表、API、归档或 WebDAV 访问。如果启动时发现没有有效 Ycloud 标记的同名目录，服务拒绝启动，不接管未知用户数据。

命名空间变更由一个异步锁串行执行。统一锁顺序为文件 I/O 配额先于变更锁，避免上传与复制/删除互相等待。具体语义如下：

- 上传写入同文件系统临时文件；已知长度一次预留，未知长度每 8 MiB 增量预留；始终保留 512 MiB 默认安全余量。
- 提交前校验实际长度、执行 `sync_all`；只允许普通文件原子替换普通文件。
- 替换先持久化事务清单并保留旧文件；启动恢复能处理未发布、旧文件已转移、新文件已发布和清理未完成状态。
- 目录复制在内部隐藏目录完成，普通文件复制后同步，最后一次重命名发布；失败不暴露半成品目标。
- 移动和复制不创建缺失父目录，也不覆盖目标。
- 永久删除先重命名进入内部待删除区，然后由单一清理任务处理；API 返回后路径立即从用户空间消失。
- 删除不提供用户回收站，内部待删除区也不等同于物理安全擦除。

无效、复合或不可满足的 Range 返回 `416` 和 `Content-Range: bytes */总长度`。

## API 与批量语义

网页单文件上传使用：

```text
PUT /api/upload?path=/完整/目标/文件名
Content-Type: application/octet-stream
```

请求体直接流式写入，不使用 multipart。单文件上限默认 5 GiB，由后台持久化设置并同时约束网页上传和 WebDAV PUT；普通下载不限制文件大小。多文件上传在浏览器中顺序执行，每个文件显示真实进度，整批结束后统一刷新一次目录。

批量移动、复制和删除采用逐项原子语义：全部成功返回 `200`，部分成功返回 `207 Multi-Status`，全部失败返回冲突状态。响应包含每项 `path`、HTTP `status`、稳定 `code` 和安全错误消息，以及成功和失败计数。成功项不回滚，前端展示失败明细。

打包下载为单任务流式 ZIP，默认最多包含 3 GiB 文件内容和 1000 个条目。字节上限只累计普通文件内容；条目上限递归合计文件与文件夹，重叠的父子选择先去重。两个值由后台持久化设置，但单打包任务、遍历硬边界和流式输出模型不可调整。文本和 Markdown 预览最多读取前 2 MiB并显示截断提示，图片及音视频交给浏览器流式处理。

## WebDAV

入口为 `/dav/挂载名称/`，支持 OPTIONS、PROPFIND、GET、HEAD、PUT、MKCOL、MOVE、COPY 和 DELETE。LOCK、UNLOCK 与 PROPPATCH 返回未实现。MOVE/COPY 不隐式覆盖目标，只读挂载拒绝全部写方法。

WebDAV PUT 使用与网页相同的原子写入器和后台单文件上传上限；无 Content-Length 时启用增量预留。其他 WebDAV 请求体最大 64 KiB。OPTIONS 只公开客户端发现需要的能力。

## 配置与恢复

`config.json` 当前 `schema_version` 为 2。缺少版本视为稳定基线格式并一次迁移到 v2；未来版本直接拒绝启动。单文件上传、打包源文件总大小和打包条目数量随账号及挂载配置一起原子持久化，旧 v2 文件缺少这些字段时使用 5 GiB、3 GiB 和 1000 条目的默认值。保存使用临时文件、同步、备份轮换和原子重命名，失败时恢复最后一份有效备份。

首次创建配置会用操作系统安全随机源生成相互独立的管理员密码和首页访问密码，配置只保存 Argon2 哈希。明文写入配置目录的 `initial-credentials.json`，日志只提示路径；管理员实际修改管理员密码或首页密码后自动删除该文件。初始化中断会复用同一凭据文件，避免配置与明文不一致。Unix 配置目录权限为 `0700`，配置、备份、临时配置和初始凭据为 `0600`；Windows 部署者必须用 ACL 限制服务账户以外的访问。

## 模块结构

| 模块 | 职责 |
|---|---|
| `bootstrap.rs` | 配置加载、监听、后台清理和优雅关闭 |
| `app.rs` | Axum 路由、中间件顺序、请求限制和嵌入资源 |
| `security.rs` | 代理边界、CSRF、Cookie、CSP 与安全响应头 |
| `auth.rs` | 会话、访问令牌、密码服务和限流 |
| `config.rs` | schema v2、校验、迁移、权限、备份和原子持久化 |
| `storage.rs` | 路径边界、流式传输、资源预留和文件操作 |
| `storage_transaction.rs` | 内部保留区、替换事务与启动恢复 |
| `file_access.rs` | 网页挂载、写权限和文件夹锁授权 |
| `api.rs` | 单文件网页 API |
| `batch_operations.rs` | 逐项原子的批量操作和 207 响应 |
| `archive.rs` | 归档预检、票据和流式 ZIP |
| `admin_api.rs` | 管理员、网页密码、传输限制、文件夹锁和 WebDAV 配置 |
| `webdav.rs` | WebDAV 认证、资源限制和方法处理 |
| `webdav_path.rs` / `webdav_xml.rs` | WebDAV 路径、Destination 与 XML |
| `frontend/src/apps/login` | 首页访问认证与顶层页面分派 |
| `frontend/src/apps/browser` | 文件浏览、上传、下载、批量操作、目录选择器及桌面/移动端菜单 |
| `frontend/src/apps/preview` | 安全类型白名单内的流式预览和受限文本预览 |
| `frontend/src/apps/admin` | 管理鉴权、统一后台布局、账户设置、登录记录、IP 限制、传输限制、文件夹锁与 WebDAV 挂载 |
| `frontend/src/shared` | 同源 API、正式路由、主题、图标与共享样式 |
| `static/app` | Vite 生成并由 `app.rs` 编译进可执行文件的生产 HTML、CSS 与 JavaScript |

## 资源模型与当前限制

目标是 1 vCPU、1 GiB VPS。密码哈希、流式归档、I/O、WebDAV、命名空间变更和待删除清理都有固定边界；上传、下载、复制和 ZIP 不把完整大文件载入内存。后台只开放三个容量/数量限制，不开放打包并发、密码并发、WebDAV 并发、事务锁或磁盘安全余量等资源保护参数。

当前限制：单实例、单 JSON 配置、内存会话、无回收站/版本历史/断点续传/集群协调/WebDAV Class 2。服务重启会要求重新登录和解锁。超过目录列表上限时结果被截断而非分页。真实用户存储不会被自动迁移或删除；上线前应先在独立测试存储完成网页及手机 WebDAV 冒烟测试。
